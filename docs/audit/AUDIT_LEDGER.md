# Kohrvellia Audit — LEDGER (resume here every session)

> **Single source of truth for audit progress.** Updated after EVERY unit so a token cutoff never
> loses more than the one in-flight unit. Rules/rubric: `01_CHARTER_AND_RUBRIC.md`. Strategy: the
> plan file `i-need-you-to-concurrent-tulip.md`.
>
> **📦 Consolidated single-file backups (token-cutoff safeguard, generated S30/S31):** **`WAVE_1_FINDINGS.md`**
> (seeds 001-041 + W1 first-hand 001-219 + register/reconciliation) and **`WAVE_2_FINDINGS.md`** (screens 220+).
> Lossless `cat` of the canonical sources (`findings/*.md` + `02_FINDINGS.md` + `ALL_FINDINGS.md` for W1;
> `findings/screens.md` for W2), which remain authoritative — regenerate the backups from them anytime.

## ▶ RESUME POINTER
- **Next finding ID:** `KV-AUD-372`
- **NEXT UP:** **★ WAVE 1 COMPLETE — engine spine + all 5 synthesis rollups regenerated from `ALL_FINDINGS.md`.** ✅ `ALL_FINDINGS.md`, `00_EXECUTIVE_SUMMARY.md`, `07_DOC_RECONCILIATION.md` (S19); ✅ `02_FINDINGS.md` (41 seeds annotated Confirmed/Refined/Downgraded/Superseded/Refuted; 029 merged→011), `03_CONNECTIVITY_AND_DEADCODE.md` (added orphans 137/099/125/122/217 + no-ops 188/191/193/194/211/181/121; **removed** the refuted audio-stub + discovery/challenges entries), `05_BALANCE_AND_BENCHMARKS.md` (B5 rewritten around 121 favor-inert; 004 0× table downgraded S1→S3; added 068 flat-growth, 150 grade-compression, 162 deep-floor-sim, 153 hybrid-mis-scale) (S20). **✅ W2-P1a DONE (S22)** — `app/dungeon/combat.tsx` **lines 1–1448** (logic half: lifecycle, `run*` action executors, Kairos turn orchestration, victory/defeat) audited first-hand → 14 findings (KV-AUD-220–233) in `findings/screens.md`. Queued cross-checks discharged: **093 confirmed per-event**, **055/165 xp REVISED→live** (consumed as proficiency, physical-biased), **177 partially refuted** (gold_bonus screen-wired), **024 refuted** (blessed selector used), **056 safe**, **057/116 confirmed** (buff consumables inert in combat). **✅ W2-P1b DONE (S23)** — `combat.tsx` 1448–3144 (presentation half) → 8 findings (KV-AUD-234–241): retreat-modal flee is a buggy duplicate of `runFlee` (234, can strand on an unrendered `'fled'` state + node un-avoided), log-length triple-mismatch 5/20/8 (235), hardcoded-color retreat modal off-token (236), **~50-entry dead-style museum** (237), inline-closure render perf (238), raw-grade-points skill scaling (239), unguarded status-badge lookup (240). **✅ W2-P2 DONE (S24)** — `room.tsx` (2036) → 9 findings (KV-AUD-242–250). **★ REVERSED AN S1: KV-AUD-242 downgrades 121 (deity favor) S1→S2** — `modifyDeityFavor` IS called at `room.tsx:497` (destructured); the W1 "0 callers" grep was a false negative → favor is inconsistently-synced, not inert. **Audit S1 count now 4** (002/080/099/190); the downgrade was propagated to `stores.md`/`ALL_FINDINGS`/`00`/`05`. Also: **243** shrine "opposed" never fires (BUG-042 casing bug), **244** `explore_secret_rooms` reachable (revises 095/178), **245** dead dungeon-shop quantified (083/172), **246** reward re-farm + bypassable No-Retreat (no `BackHandler`), 247 inline balance/hex, 248 shrine blood-offering death hole. **✅ W2-P3 DONE (S25)** — `floor.tsx` (1522) → 8 findings (KV-AUD-251–258). Confirmed the **player-facing half of the S1 080** (251: backward nodes render tappable GO badges + tooltip "Go Here"; `moveToNode` has no directionality guard) + **082** (252: ascend re-rolls — the Alert says "a new path will be generated") + **232** (253: per-floor-conduct behavements skip normal descents, fire only on boss kills). **Discharged 086** (257: `enterFloor` is descend-gated — no over-tick; the `:415` as-any cast confirms 218). New: mid-run "Menu" escape without `endRun` (254), the off-token "second palette" recurs across combat/room/floor (255), map-render perf + dead styles (256). No severity changes (080 already S1). **✅ W2-P4 DONE (S26)** — `level-up.tsx`+`denatus.tsx`+`job-select.tsx` → 5 findings (KV-AUD-259–263). **★ NEW S1: KV-AUD-259 — the Level-10 Denatus/Paragon climax is UNREACHABLE.** A routing audit found **nothing routes to `/dungeon/level-up`** (dead, superseded by `ascension.tsx`); denatus's only inbound route is the dead `level-up.tsx:309`; the live `ascension.tsx` has **no L10 branch** (`handleComplete`→town) and never calls `performDenatus`/`setParagonTitle` → **`character.paragonTitle` is ALWAYS null** → the entire Paragon buff layer + soul-climax (011/176/178/093/097/179) is dead in practice. **Audit S1 count 4→5** (002/080/099/190/**259**); propagated to `00`/`ALL_FINDINGS`. **260** level-up.tsx dead (855 LOC; confirms 009/027/133 in dead code); **261** denatus latent defects (097/179 agency, raw-points top-stats, mount-vs-finish strand, 099 recordParagon-never-called); **262** job-select half-reachable (base-mode dead, spec-mode live via `ascension:204`). **✅ W2-P5 DONE (S27)** — the 7-screen dungeon bundle → 6 findings (KV-AUD-264–269). **264 (S2)** `_layout.tsx` sets **no `gestureEnabled:false`** → iOS swipe-back is live on combat/room (which guard only Android hardware-back via `BackHandler`) — the **root** that makes the 234/246/254 nav-integrity cluster exploitable on iOS. **265 (S2)** the milestone-boss "FIGHT" doesn't start combat directly — `monster` is null (no `prepareEncounter` on the floor→boss path, only `setBossSnapshot`) so it falls back to `room`→`encounter` (fragile 2-screen detour). Confirmed cleanly: 058 (`startCombat` dead), 138 (protection upstream in prepareEncounter), **BUG-022/034 RESOLVED** (boss-dialogue achievements wired, 266), BUG-031 (armor placeholder, 267), BUG-023 (boss-cleared works), 188 (enchant display-only); revised 215 (boss favor-sense varies now, via 242). No new S1s (count stays 5). **✅ W2-P6a DONE (S28)** — `town/familia/{blessing-rite,index,ascension}` → 4 findings (KV-AUD-270–273). **★ FAVOR PICTURE COMPLETE — KV-AUD-270 (S2, completes 242): favor→power is shrine-ONLY.** A grep of `town/familia/*` for `modifyDeityFavor|adjustFavor` = **0 hits** → the Blessing Rite, Familia Home, Ascension, and God-Challenges (the entire intended patron loop) **never touch `character.deityFavor`**; the only caller in the game is `room.tsx:497` (shrine). So the DanMachi favor relationship is power-inert except incidental shrine visits (the main loop is dead; stays S2 since shrines work). (122 refuted: `familia:306` displays `FAVOR_STATUS.label`, not the dead `.multiplier`.) **271 (S3)** ascension hardcodes "LEVEL 2" (`:278`) + has no L10 phase → on-screen reconfirmation of 259. **272 (S3)** blessing-rite minors (dead `newValue` field, phantom `DOMAIN_GLYPH` domains, flat-growth 068 manifests here, incomplete Falna formula). Positives (273): eviction (Option C) + challenges wired (127), a relic-reveal UI (partial 208 refute), primary-stat agency (unlike denatus 261), strong deity voice. No new S1s. **✅ W2-P6b DONE (S29) — ★ W2-P6 town COMPLETE** — `town/{index,_layout,character/index,inventory/index}` → 4 findings (KV-AUD-274–277). **274 (S2)** the Character **Status screen uses the UNBLESSED `getDerivedStats`** (`:94,125`) while combat/dungeon-inventory/familia/hub all use `getDerivedStatsWithBlessings` → the canonical stats view diverges from actual combat stats. **275 (S3)** Status screen shows the phantom `STAT_INFO.combatEffect` (149) + a second hardcoded grade-color palette (≠ `Colors.grade.*`). **276 (S3)** town inventory confirms the 026 casts (`:401,407,446`) + renders 6 phantom (unfillable) armor slots, contradicting the dungeon inventory's honest placeholder (267). **277 (S3)** town `_layout` gesture pattern (264) + `enterDungeon` resumes a dangling run (BUG-013, links 254); good permadeath warnings. No new S1s. **✅ W2-P7a DONE (S30)** — `town/guildhall/index.tsx` (1388) → 3 findings (KV-AUD-278–280), mostly confirmations. **278 (Info)** discovery renders all 4 states (134), Gem Exchange `category==='gem'` (BUG-037) + `resource_sell_items` (BUG-030), the Living-Economy market board (111), ascension→familia (`:318`); the one defect — `checkReputationDiscovery` uses the **general SHOP rep** (`:283`) as the single discovery track (confirms 198). **279 (S3)** the ascension stat-gate uses raw within-grade `s.points >= 500` mislabeled "Grade D" (`:288`) — the **5th instance of the systemic raw-points-vs-grade bug class** (261/218/level-up/boss-encounter); deity "approval" is a free rubber-stamp button. **280 (S3)** hardcoded `TIER_COLORS` — a 4th off-token palette. No new S1s. **✅ W2-P7b + W2-P8 DONE (S32–S33).** **P7b shops → KV-AUD-281–285** (★ R1 self-correction 281: **armor IS buyable + equippable** — refutes 108, corrects my own 276/267; armor defense applies, real defects partial: penalties dead 193, accessory2 076, stale dungeon placeholder 267; **282** the 113 full-bag guard is ineffective → silent gold-loss reachable; **283** BUG-038 ✓ / 105 ✓ / 219 refuted / 205 revised; 284 off-token palettes). **P8 blacksmith → KV-AUD-286–288** (★ R1 revision 286: **`repair.tsx` is functional SALVAGE, not a no-op** — revises 211; the durability model stays dead but the screen works + closes the loot loop; **287** confirms 119/114, mitigates 118; 288 confirms 107). No new S1s (count stays 5). **✅ W2-P10 DONE (S35) — ▰▰▰ WAVE 2 COMPLETE ▰▰▰** — framing layer (tutorial/settings/codex/title/root) → 7 findings (KV-AUD-291–297). **291 (S3)** tutorial trim left 4 orphaned screens (combat/stats/falna/leveling, ~1015 dead LOC; confirms AUD-012). **292 (S3, NEW)** Haptic-Feedback toggle is a placebo (local `useState`, never read by `useHaptics`). **293 (S3)** volume dual-source confirmed (144 — dead `useGameStore.musicVolume/sfxVolume`). **294 (Info)** settings cheats are `__DEV__`-gated (010 refuted for production). **295 (Info)** title epitaph memoized + `clearAllStores` on every New Game path (009 resolved); stale `v0.1.0` string. **296 (S3)** codex hides all ~15 L10 achievements (`LEVELS=[1..9]`; corroborates 259) + 3/4 stub categories. **297 (Info)** root `ErrorBoundary` positive; lone off-token hex + SafeAreaProvider doc mismatch. No new S1s — **Wave-2 S1 count stays 5 (002/080/099/190/259)**. **✅ WAVE 3 STARTED (S36) — combat-data (DC) cluster sequenced first** (it discharges the content cross-checks 163/165/166/168 + supplies the 162 sim inputs; DP pantheons / DI items / DPr progression deferred). **✅ W3-DC1 DONE (S36)** — `data/monsters/baseMonsters.ts` (1231, 36 monsters CR 0.5–16.0) → 5 findings (KV-AUD-298–302) in `findings/data_combat.md`: **298 (S2, NEW)** poison is negated by ~53% of the bestiary (17/36 immune + 2 resist) → poison builds non-viable (the data side of 189/051); **299 (S3)** `getRandomMonsterForFloor` (live @ `useCombatStore:269,315`) is unweighted uniform-random + the `biomes` field is **dead** (0 readers; compounds 169); **300 (S3)** vestigial `xpValue` ×36 (confirms 165); **301 (S3)** thin 5-suffix/7-prefix set with dead suffix `statusEffect`/passives (052/188/190); **302 (Info)** **163 LATENT** (no `baseCR:0`; min final CR ≈0.2), baseHP 35→1000 recorded for the 162 sim, deep floors 70–100 draw from only ~7 monsters. No new S1s (stays 5). **✅ W3-DC2 DONE (S39)** — `monsters/{monsterArt,monsterFlavor,index}` + `combat/combatNarration` → 4 findings (KV-AUD-307–310), all grep-/diff-verified for connectivity (the decisive lens for a content layer). **307 (S3)** `getStatusAppliedNarration`/`getStatusDamageNarration` are **DEAD** (not imported by `useCombatStore:39-43`) → the 24-line `STATUS_APPLIED`/`STATUS_DAMAGE` pools never show; combat emits inline status text. **308 (S3, machine-diff)** **12 of 36 monsters have no ASCII art** — exactly the CR 7–16 deep/boss tier (`fire_giant/storm_giant/pit_fiend/red_dragon/lich/behemoth/titan/demon_lord/void_dragon/primordial/death_knight/ancient_wyrm`) → all render the identical generic fallback face (bounded by 303; graceful). **309 (S3, discharges 168)** `SUFFIX_FLOOR_GATES`/`isSuffixAllowedOnFloor` is **doubly dead** — 0 callers AND gate keys (`burning/venomous/chilling…`) share zero overlap with suffix ids (`of_flame/of_venom/…`) → always falls through to gate 1; the live floor-gate is the data's own `MonsterSuffix.minFloor` (divergent-duplicate, P3). **310 (Info)** positives: `getMonsterFlavorText` is wired (`encounter.tsx:60`) AND all 7 prefix + 5 suffix display-names match the flavor keys exactly (no silent-null); 4/6 narration helpers wired AND consumed via `addLogEntry`; `STATUS_DAMAGE` correctly omits non-DoT statuses; voice is on-register; minor nits = incomplete `index.ts` barrel + flavor shows only on the encounter preview (boss path bypasses it, 265). No new S1/S2 (stays 5). **➡ NEXT UP = W3-DC3 weapons** (`data/weapons/*` — baseWeapons 831 + stat variants + index; carries 185/153 hybrid-set spellings `CHA_INT`≠`INT_CHA` + dead pairs, 119 `maxOutputCap` on upgrade, 206 `damageType` canonicalization; pair with the Layer-C zod schema extension to deities/weapons). First-hand findings resume at **KV-AUD-311**. **✅ W3-DC3 DONE (S40)** — `data/weapons/*` (338 weapons; tsx validator imported the REAL `ALL_WEAPONS`) → 5 findings (KV-AUD-311–315). **★ The machine pass reframed the whole hybrid thread.** **311 (S2, NEW)** **`specialMechanic` is dead** on **111/338 weapons incl. all 34 uniques** — grep finds only the type def + the sacred-conversion *writer*, **0 readers** (no combat handler, no unique-id switch, no display) → the "Loot is King" pillar #2 is hollow (Godslayer crit-chain / World Ender / etc. neither execute nor show). **312 (S2, NEW, recontextualizes 153/185/061/043)** **dual-stat scaling never engages** — 0/338 weapons use a hybrid `category`; all are single-category + a `primaryStats:[A,B]` array, but the gate `isHybridMixed = HYBRID_MIXED_CATEGORIES.has(category)` (`useCharacterStore:184`) is **always false** → `physRatio` always 1 → `weaponMagic=0` → 162 weapons' second stat is inert; **STR_INT/AGI_WIS "imbued" hybrids drop their magical half entirely**. 185 = real type-debt but **moot** (no hybrid categories in data); 153/061 latent. **313 (S3)** discharges: 119 store-only (`createWeaponInstance:749` computes `maxOutputCap` correctly), 206 clean for base weapons (all damageTypes canonical), no dup ids; stale `hybridWeapons.ts:4` "320 weapons" comment (really ~162). **314 (S3)** LCK under-provisioned (22 vs 40/cat) + `twoHanded` cosmetic (no off-hand slot). **315 (Info)** positives: cleanest data integrity yet (no dup/enum/refint defects), pool wiring solid (`generateLeveledWeaponDrop` level-gates), range/twoHanded/attackSpeed display-wired, outstanding weapon-name + mechanic *voice* (tragic it's unwired). No new S0/S1 (stays 5); **+2 S2** (311/312, the most pillar-damaging of Wave 3). **➡ NEXT UP = W3-DC4 bosses** (`milestoneBosses` 910 + `milestoneBossesFloors30to60` 1400 — carries 163 `baseCR:0` re-check, 215 boss favor-sense, the 218/261/279 raw-points stat-gate class; harness-validate the boss tables). First-hand findings resume at **KV-AUD-316**. **✅ W3-DC4 DONE (S41)** — `data/bosses/{milestoneBosses 910, milestoneBossesFloors30to60 1400}` (20-boss roster imported via the REAL `getMilestoneBoss`) → 5 findings (KV-AUD-316–320). **316 (S2, NEW)** **boss signature mechanics are display-only** — all 20 bosses define a `mechanic` (Vanya's Corrosive Shell, Ulfbiik'ok's Pack Threshold) shown at `boss-encounter.tsx:232-233` + "revealed" by Observe, but combat **never reads `boss.mechanic`** and the fight is a regular generated monster (265) → the game teaches a puzzle the fight ignores (reachable pre-wall for floors 5/10/15; twin of 311/188). **317 (S3)** CLAUDE gap #6 "floors 30-100 have no bosses" is **STALE** — `getMilestoneBoss` aggregates all 3 files; all 20 milestone floors (5-100/5) wired + authored (but 15 are past the 303 wall → authored-yet-unreachable). **318 (S3)** **61 raw-points stat-gates** (`statRequirement`/`statCheck` `minPoints` 50-150, modal 100×26) → grade-blind boss secret-outcome gating (6th site of the 218/261/279 class). **319 (S3)** `MilestoneBoss` has **no baseCR/baseMonsterId/stats** → 163 N/A for bosses; favor/archetype-sense reads `PlayerSnapshot` (varies, per 242). **320 (Info)** ★ the boss layer is the **strongest content asset** — pristine refint (20/20 floors, 0 dup ids, 0 floor mismatches, boss achievements resolve 6/6), outstanding voice at every depth, sophisticated archetype-sensing design. No new S0/S1 (stays 5); +1 S2 (316). **➡ NEXT UP = W3-DC5 bosses** (`milestoneBossesFloors65to100` 1485 — floors 65-100; confirm the `'—'` pantheon + lowercase `vituna'sta` hygiene; same display-only-mechanic + raw-points pattern expected — then the W3-DPr progression cluster). First-hand findings resume at **KV-AUD-321**. **✅ W3-DC5 DONE (S42) — ▰▰▰ BOSS CLUSTER COMPLETE ▰▰▰** — `data/bosses/milestoneBossesFloors65to100` (1485, floors 65-100, 8 bosses) → 4 findings (KV-AUD-321–324). **321 (S3, automated; revises 266)** **5 of 8 deep boss-outcome achievements are dangling refs** — `kohr_uwa_split_resolved/haitt_lox_war_read/ulfkon_naa_stayed/vituna_sta_endured/ekva_baniyata_cycle_read` (floors 70-90) exist in **0** files → `boss-encounter:151 discoverAchievement(id)` silently no-ops; "boss achievements wired" (266) holds for floors 5-60 but breaks at 65-100 (machine-caught; the eye saw a plausible string). **322 (S3, synthesis 303×316×259×302)** the **authored floor-100 finale (Skaervox, "The Lawmaker") is triply unreachable+unimplemented** — ~85 floors past the wall (303), its mechanic ("the fight ends at 1 HP… there is only the quality of the attempt") is display-only (316) + the fight is a generic deep monster (302), and the L10 paragon crown is dead (259); the entire point of the climb can never be experienced — the apex of the audit's theses. **323 (S4)** hygiene: lowercase `vituna'sta` name (floor 85), intentional `pantheon:'—'` (Skaervox, beyond pantheon), `ChoiceTag` not imported (:15, harmless), invented pantheons (Johr'kuun/Rutkean'i) with no deity backing (lore). **324 (Info)** ★ the Skaervox finale is the project's best writing (the 11 Laws / Aleabishal / Máalkohr worldbuilding); the 20-boss cluster is **complete content** — pristine structure (0 dups, full coverage, 100% authored), best narrative asset, defects only in the runtime/completion layer beneath. No new S0/S1/S2 (stays 5 S1; 316 was the cluster's lone new S2). *(Catalog note: there is no `bosses/index.ts` — aggregator is `getMilestoneBoss`.)* **★ COMBAT-DATA (DC) CLUSTER COMPLETE: W3-DC1…DC5, KV-AUD-298–324.** **➡ NEXT UP = W3-DPr1** (`data/achievements/level{1,2,3}` — begins the progression cluster; discharges 196 [~11 dead RequirementTypes] + 130, and cross-checks the boss achievement refs 321 against the full registry). First-hand findings resume at **KV-AUD-325**. **✅ W3-DPr1 DONE (S43) — progression cluster begins** — `data/achievements/level{1,2,3}` + registry-wide validation (`.audit_tmp/ach_check.ts` imports the REAL `ALL_ACHIEVEMENTS`) → 4 findings (KV-AUD-325–328) in **new `findings/data_progression.md`**. R1: the FIRED `RequirementType` set was re-derived first-hand from the actual call sites = **exactly 196's 10 fired / 11 dead**. **325 (S2, discharges/quantifies 196/031/130)** **20 of 123 achievements (16%) are uncompletable** via the 11 dead types (all 11 used; every level affected; tiers std 4/chal 10/hero 1/leg 3/myth 2) — incl. **4 STANDARD-tier shown to the player as achievable** (`l1_survivor`/`l2_safe_return` floor_return, `l3_skilled_fighter` skill_use, `l4_safe_deep_return`). **326 (S2, sharpens 130→definitive)** **the entire MYTHIC tier is uncompletable** — 11 mythic + 2 legendary use `discoverySource:'undiscovered'` which **no discovery path triggers** (grep: 0 handler; rep-discovery keys on NPC sources; only STANDARD auto-reveals) → born hidden → 0 progress (130 catch-22); compounds 178 (Mythic paragon impossible) + 296 (codex hides L10) = the prestige capstone is dead 3 ways. **327 (S3, NEW)** the `custom` type has **no per-id checker** — 20 custom achievements progressed only by the type-blind `updateProgress('custom',1)` ×6, all in `boss-encounter` → complete on unrelated boss triggers; ~8 non-boss custom achievements only progress by fighting a boss. **328 (Info, discharges 321)** registry refint clean (123, 0 dups); **321 re-confirmed** (5/10 boss ids present, the 5 deep ones absent from `ALL_ACHIEVEMENTS`); level 1-3 voice excellent (First Blood/Deathless Dawn/The Impossible Novice/Flawless Descent). Net: no new S1 (stays 5); **+1 S2 (326)**; 325 discharges 196; 327 new S3. Practically **~27% of the achievement system is unreachable** (16% dead-type + 13 undiscoverable). **➡ NEXT UP = W3-DPr2** (`data/achievements/level{4,5,6}` — voice; registry data already validated). First-hand findings resume at **KV-AUD-329**. **✅ W3-DPr2 DONE (S44)** — `data/achievements/level{4,5,6}` (mid tiers; voice pass, data already validated in DPr1) → 3 findings (KV-AUD-329–331). **329 (S3, NEW)** **conduct/constraint achievements (6 `eq` + 3 `lt`) are broken by cumulative counters** — `isRequirementMet` *correctly* implements the comparison operators (positive), but they compare against monotonic lifetime counters: "Untouchable Champion" (`damage_taken eq:0`) actually demands never taking ANY damage all game (not "no boss damage"); `lt` constraints ("less than 100 damage on floors 5-8", "speedrun <20 kills") are true at progress=0 and break as the counter climbs — a **third** uncompletable vector, on LIVE types (distinct from 325 dead-types / 326 undiscoverable). **330 (S4)** `stat_reach value:4` conflated (= "Grade D" vs "4 stats" across entries). **331 (Info)** mid-tier voice strong (Into the Depths/Centurion/Void Scholar; "the fastest blade is the one that never swings"), requirement values ramp sensibly; **sharper read of 325** — several dead-type achievements name **unbuilt systems**: "Stealth Master" (no stealth system exists), "Deity Favored" (favor near-inert 270) — the achievement layer is partly a fossil of cut/unwired features. No new S1/S2 (stays 5); +1 S3 (329). **➡ NEXT UP = W3-DPr3** (`achievements/level{7,8,9}+index` — prestige/L10 tail incl. the undiscoverable Mythic capstone 326). First-hand findings resume at **KV-AUD-332**. **✅ W3-DPr3 DONE (S45) — ★ ACHIEVEMENT REGISTRY COMPLETE** — `data/achievements/level{7,8,9}+index` (prestige/L10 tail) → 3 findings (KV-AUD-332–334). **332 (S3, sharpens 326)** **the Mythic capstone is quadruple-dead** — all 11 mythics gate on floors 25-100 (past the 303 wall): Deathless Depths(25)/Ultimate Warrior(30)/The Paragon Path(50)/The Anomaly Noted("Floor 100, stand before Skaervox")/Skaer'kohr'wilak(9 GLORY behavements) → dead 4 ways at once (undiscoverable 326 + floor-gated 303 + dead-climax 259/316/322 + unreachable behavements 178). **333 (S3, NEW)** **`custom value:0` "Without dying" is a no-op** — default `gte` → `progress≥0` always true (+ no custom checker 327) → the deathless conduct clause never enforced; a reusable broken idiom. **334 (Info)** high-tier voice is the system's best (Slaughter→Apocalypse→Armageddon→God Slayer; the boss achievements tie L7-10 to the deep-boss arc); the L10 layer is the audit's **densest dead zone** (corroborates 259/296/303/326). **★ 123-achievement registry fully audited (DPr1-3, KV-AUD-325-334):** authoring excellent, but **~27%+ unreachable across 4 vectors** (dead types 325 / undiscoverable 326 / type-blind custom 327 / conduct-constraint 329/333), densest at L10. No new S1/S2 (stays 5); +2 S3 (332/333). **➡ NEXT UP = W3-DPr4** (`data/jobs/{jobDefinitions(1338),specializations,index}` — carries 202 `makeJobKey` sort-trap; harness-validate job keys + starter-skill/weapon refs). First-hand findings resume at **KV-AUD-335**. **✅ W3-DPr4 DONE (S46)** — `data/jobs/{jobDefinitions 1338, specializations 523, index}` (51 base + 18 spec; tsx validator imports REAL `JOB_DEFINITIONS`) → 5 findings (KV-AUD-335–339). **335 (S2, NEW)** **35 of 56 stat-builds (63%) have NO job** — `getJobsForStats` covers only 21/56 triples; a missing-triple character reaches L2 **job-less** (no starter skill/bonus/spec) — gracefully handled (ascension:297 "Continue" fallback, no soft-lock) but breaks "every stat matters" + contradicts the doc "2-4 per combination" (35 have 0, 3 have 1). **336 (S2, NEW, job-side of 199)** **support archetype starter skills are inert** — 13/51 jobs use the dead `buff` effect; champion ("Battle Cry") + monk ("Focused Chi") are **buff-ONLY → complete no-ops**; the class-defining ability does nothing. **337 (S3)** 6 base↔spec **dup job ids** (warlord/crusader/arcanist/enchanter/hunter/skirmisher). **338 (S3, REFUTES 202)** `getJobsForStats` sorts BOTH sides via makeJobKey ("order does not matter") → 202 "unfindable if unsorted" doesn't hold; + sparse specs (only 9/51 base jobs branch). **339 (Info)** clean: inline starterSkill + StatName weaponCategory = no dangling refs; spec parentJobId 0 dangling; job selection uses **effStat** (not raw points — avoids 261/279 class); strong voice. No new S0/S1 (stays 5); **+2 S2** (335/336). **➡ NEXT UP = W3-DPr5** (`data/skills/* + consumables + materials + loot/lootPools + events/dungeonEvents(863) + ramifications + weaponRegistry` — final data unit; carries 199/204 + the 302 loot-pool ref cross-check). First-hand findings resume at **KV-AUD-340**. **✅ W3-DPr5 DONE (S47) — ★ DC/DPr DATA CLUSTER COMPLETE** — `skills + consumables + materials + loot/lootPools + events/dungeonEvents(863) + ramifications` (tsx validators import REAL catalogs; handler-sets re-derived R1) → 3 findings (KV-AUD-340–342). **340 (S3)** **loot carries 2 dead layers** — monster `lootTable` (22 refs) dead (loot resolves by `MonsterCategory`) + pool `materials`/`consumables` fields dead AND broken (16 dangling material refs, `hide` vs `monster_hide` naming mismatch; combat computes materials its own way :1490-1523); only weapon-cat/gold pool fields live. **341 (S3, Likely)** **event/ramification negative-status outcomes partly inert** — `weapon_chip` ramification uses dead `buff` + non-canonical `weapon_damage_down` statusId (190); travel drops them (268); resource drains (gold/ration/HP) work, status-afflictions don't — the effect-resolver gap reaches the content layer. **342 (Info)** R1 handler re-derivation confirms **199 exactly** (only buff/damage_percent/flee dead); **199 refined** (skill catalog 12 clean — dead impact is entirely job buff-skills 336); **204 → LATENT** (no consumable uses reveal/identify); materials/consumables refint clean (18/19, 0 dups); dungeonEvents (53, wired via room.tsx) are a genuine "Meaningful Choices" win (moral dilemmas + stat-check risk/reward, strong voice). No new S0/S1/S2 (stays 5 S1); +2 S3. **★ COMBAT-DATA (DC1-5) + PROGRESSION (DPr1-5) COMPLETE = KV-AUD-298–342.** Remaining W3: **DP pantheons** (19 files ~24K) + **DI items/relics** (discharges 099). **➡ NEXT UP = W3-DP1** (pantheons) or DI items (099 discharge). First-hand findings resume at **KV-AUD-343**. **✅ W3-DI DONE (S49) — ★ discharges the S1 099 with a hard count** — `src/data/items/*` (588 sacred items; `.audit_tmp/relic_check.ts` imports the REAL `ALL_DEITY_RELICS`+`DOMAIN_ARTIFACTS`+`ALL_PANTHEON_PIECES`) → 4 findings (KV-AUD-343–346) in **new `findings/data_items.md`**. **343 (S1, discharges/quantifies 099 — stays S1)** the sacred reward tier is **multiply dead**: **244/588 (41%) UNOBTAINABLE** (acquisition uses a dead metric — the 13 `return 0` stubs + 4 no-writer metrics `paragon`/`event_success`/`skill_uses`/`skill_sp_spent`, re-confirmed 0 callers); **15 false-unlocks** (`value:0` on a dead metric auto-passes a challenge req); **all 490 deity relics `isSecret`+`revealFavorRequired:100`** → never revealed (208, read by 0 stores) + favor never hits 100 (270); **all 588 passives decorative** (210 confirmed — `passiveId` 0 consumers; `passiveDescription` only copied into the dead `specialMechanic` 311). **344 (S3, quantifies 206)** 107/245 relic weapons use non-canonical `damageType` (`physical`/`chaos`). **345 (S3)** acquisition `targetType` nuance (`different_characters_floor25`/`player_initiates_elite`) is dropped by the evaluator → relics unlock on cruder conditions than stated. **346 (Info)** ★ the relic tier is the **most ambitious authoring in the game** (245 hand-voiced per-deity conditions + cryptic `deityRevealText` — Hades/Poseidon/Vanya) and structurally clean (0 dup ids) — its deadness is the purest "models more than it wires" tragedy; doc *undercount* (245 pairs/490 items vs index's "168/336"). **099 stays S1** (reinforced). No new S0/S1/S2. *(Scope: relic DATA 100% machine-validated; voice sampled across Slavic/Greek; exhaustive per-relic voice read deferred — 208 makes the prose unreachable.)* **➡ NEXT UP = W3-DP pantheons** (`src/data/pantheons/*` ~24K LOC — deity content/voice; confirms relic→deity refint + seed 028 maya/inca + `.tmp`→`.ts` gap-#4) → then Wave 4. First-hand findings resume at **KV-AUD-347**. **✅ W3-DP DONE (S50) — data-integrity complete; voice sampled** — `src/data/pantheons/*` (310 deities, 17 active pantheons; `.audit_tmp/deity_check.ts` imports REAL `allDeities`+`ALL_DEITY_RELICS`) → 5 findings (KV-AUD-347–351) in **new `findings/data_pantheons.md`**. **347 (S3, ★ REFUTES seed 028)** **maya & inca are complete + valid but ORPHANED** — both export `mayaPantheon`/`incaPantheon`, type `Deity[]`, **tsc exit 0**, full structure + rich voice; the index's "TODO: Regenerate with correct interface" is **stale** (they were fixed, never re-wired) → ~28 authored deities dead (P7); reframes CLAUDE gap #4 (5/7 .tmp wired, maya/inca completed-but-orphaned). **348 (S3)** 4 dup deity ids across overlapping pantheons (`vayu` hindu∩persian; `fujin`/`raijin`/`yama_no_kami` japanese∩shinto) → `getDeityById` shadows the 2nd; 310 = 306 unique. **349 (S3, discharges DI-deferred refint)** relic→deity: 240/245 resolve; **5 dangling** = the milestone bosses (vanya/sorath/kutcher/kalindi/malik, not patron deities); 66/310 deities have no relic. **350 (S3, doc-recon)** **310 deities / 17 pantheons vs docs "168 / 12"** — ~2× undercount; but all 14 domains covered + all stats valid (clean enum). **351 (Info)** ★ deity data is the cleanest refint on its core axes + excellent voice (Zeus/Itzamna). No new S0/S1/S2 (stays 5 S1). *(Scope: refint 100%; voice sampled greek/maya/inca; exhaustive 310-deity voice read deferred as low-yield.)* **★ WAVE-3 DATA INTEGRITY EFFECTIVELY COMPLETE** (DC + DPr + DI + DP-refint = 298–351). **➡ NEXT UP = Wave 4** (components/53 · constants+hooks · config/docs · the knip/ts-prune/madge tool sweep validating the 03 §6 dead-code inventory · final synthesis) — or the optional exhaustive DP voice pass. First-hand findings resume at **KV-AUD-352**. **✅ W4-4 TOOL SWEEP DONE (S51) — ★ WAVE 4 BEGINS; machine-validates the dead-code inventory** — `tsc`/`madge`/`knip` executed against the game tree (`.audit_tmp/knip.jsonc`, `entry:['app/**']`) → 4 findings (KV-AUD-352–355) in **new `findings/wave4.md`**. **352 (S3, reinforces S0+P4)** **`tsc --noEmit` = 0 errors = FALSE GREEN** — passes only because the 3 untracked S0 files exist on disk (CI breaks) + the `as any`/`as never`/`category:string` casts suppress the schism/dual-stat/category-drift errors (190/166/312/206/218); the compiler is blindfolded. **353 (S3)** **madge: 23 circular deps** — 3 RUNTIME store cycles (`character↔sacred`, `character↔shop`, 4-hop `character→shop→consumables→inventory`; the inline-`require()` hacks don't hide them) + 20 type-import cycles (relic/boss files import their type from the aggregator). **354 (S2, ★ validates 03 §6)** **knip CONFIRMS the audit's orphans** — unused-exports flags `getStatusApplied/DamageNarration` (307), `getMaterialsForMonster/rollMaterialDrop` (340), `isSuffixAllowedOnFloor` (309), `getCarryCapacity` (159), `calculateGoldDrop` (164), `applySoftCap` (148); unused-files flags maya/inca (347) + types barrel (217). NEW: **`starterSkills.ts` dead** (380-line catalog; job skills inline — extends 342), **7 component orphans** (DeityFilters/ActionModal/EmphasisText/TowerWarningModal/StatTooltip + barrels — pre-validates W4-1), **6 dead barrels**. knip LIMITATION: can't flag dead `app/**` routes (entries) → 260/291 needed manual route-graph (knip complements). **355 (Info)** the sweep validates "execute, don't just read" (~10 reading findings machine-re-derived); promote `knip.jsonc`+madge to `audit/integration/` + a CI gate (closes P7/P4 mechanically). No new S0/S1 (stays 5; 352 reinforces S0). **➡ NEXT UP = W4-1 components** (53 files first-hand; discharges 033/008/palette-count, with knip's 7 orphans as a head-start) → W4-2 constants/hooks → W4-3 config/docs → W4-5 final synthesis. First-hand findings resume at **KV-AUD-356**. **✅ W4-1 DONE (S52) — components (49 .tsx), machine pass + sample** → 4 findings (KV-AUD-356–359) in `findings/wave4.md`. **356 (S3, discharges prior-agent 033)** **0 of 49 components use `React.memo`** (only 1 useMemo/useCallback) → no memo boundaries → heavy combat/floor/room screens re-render their whole tree on any store change (compounds 230/238/256); partly mitigated by native-driver ref-animations (render still runs). **357 (S3, discharges seed 008)** **no custom font loaded** (no useFonts/Font.loadAsync; only 'monospace' for ASCII) → the "gothic serif" aesthetic pillar is unimplemented (system fonts); explains epitaph `fontFamily:undefined` (268). **358 (S3)** **66 hardcoded hex across 7 components** (off-token theme reaches components) + the 7 knip-confirmed dead components (DeityFilters/ActionModal[refactor-orphan]/EmphasisText/TowerWarningModal/StatTooltip+barrels) + `MonsterDisplay.getMonsterTell` has 4 dead branches (defensive/summoner/support/regenerator — patterns no monster has, extends 305). **359 (Info)** ★ components otherwise well-built — token-disciplined, native-driver animations off useRef, 14 domain-icons all used, dumb-presentational per CLAUDE; defects are systemic not malformed. No new S0/S1/S2 (stays 5). *(All 49 machine-covered; MonsterDisplay read first-hand; exhaustive per-component read deferred — presentational + machine-validated.)* **➡ NEXT UP = W4-2 constants/hooks** (Typography font decls? GameConstants live/dead per 065/003; useHaptics 292/useCreationState) → W4-3 config/docs → W4-5 final synthesis. First-hand findings resume at **KV-AUD-360**. **✅ W4-2 + W4-3 DONE (S53) — constants/hooks/config** → 4 findings (KV-AUD-360–363) in `findings/wave4.md`. **360 (S2, definitive discharge of 003/065/151)** **`GameConstants` is ~80% DEAD** — only 4 of 20 exported configs are imported (`Combat`/`Loot`/`ZONE_NAMES`/`DENATUS_WILAK_ALIGNMENT`); the 16 dead incl. `DerivedStatFormulas`/`WeaponBalance`/`MonsterBalance`/`Deity`/`ProficiencyThresholds`/`ShopPrices`/`SoulSystem`… → the documented "single source of balance truth" / "never hardcode" law is systematically violated (balance hardcoded in Stats.ts 151 + stores/screens); tuning GameConstants has NO effect → balance is **untunable from the documented source**. **361 (S3, discharges 357+292 from source)** Typography declares **no serif font** (only MONO; gothic-serif aesthetic approximated via weight/spacing/caps) + `useHaptics` fires **unconditionally** (no settings check → the 292 toggle is a confirmed placebo). **362 (Info)** ★ `Colors` is a solid 13-group token system — the off-token palettes diverge from tokens that EXIST (mechanical fix); biome group has dead void/dreamscape/labyrinth (169). **363 (S3, discharges seed 012)** **17 junk files litter+track the repo root** (`0`/`3)`@1037b/`w.category`/`Math.max(1`/… botched redirects = the git-status D/M churn); config files all present (app.json/babel/tsconfig/deploy.yml/.gitignore). No new S1; 360 quantifies the existing 003/151 S2. **★ WAVE-4 FIRST-HAND READING COMPLETE** (W4-1 components · W4-2 constants/hooks · W4-3 config + W4-4 tool sweep). **✅ W4-5 FINAL SYNTHESIS DONE (S54) — ★★ THE AUDIT IS COMPLETE.** All units (Waves 1–4) audited first-hand + machine-verified: **KV-AUD-001…363** · 1 S0 · 5 S1 (002/080/099/190/259) · ~70 S2 · ~95 S3 · ~50 Info · **0 game-code edits (report-only).** `06_REMEDIATION_BACKLOG.md` is the leverage-ordered action plan; `00`/`02`/`03`/`04`/`05`/`07`/`08`/`ALL_FINDINGS` are the synthesis rollups (current through 363); `findings/*.md` are the canonical first-hand prose; `audit/` + `.audit_tmp/` are the re-runnable harness. **No NEXT UP — the audit scope is complete.** Optional (owner's call, low-yield): exhaustive 310-deity + per-component voice reads. *Next ID stays 364 (synthesis added no findings).*
- **Mode:** report-only · first-hand full re-read · engine-first · sequential (no agent swarms)

## How to resume (per-session runbook)
1. Read `01_CHARTER_AND_RUBRIC.md` (rules R1–R10, lenses L1–L10, schema).
2. Read this ledger → take **NEXT UP**; mark it **WIP**.
3. Read the unit's file(s) **fully, every line**; apply all 10 lenses; trace connectivity; confirm or
   refute any **seed** finding (see below) or prior-audit claim touching those files.
4. **Append** findings (continue IDs from "Next finding ID") to the unit's `findings/*` file.
5. Mark unit **DONE**; bump Next finding ID; set the new **NEXT UP**; add a Session-log line.
6. Continue to the next unit while budget allows, else stop — state is already saved.

## Status legend
`TODO` not started · `WIP` in progress (redo if interrupted) · `DONE` audited first-hand & findings written.

## Seed findings (provisional — must be confirmed first-hand)
`02_FINDINGS.md` holds **41 provisional findings (KV-AUD-001…041)** from the initial triage. They are
NOT accepted as coverage. Each is re-confirmed/refuted when its unit is audited; confirmed ones are
rewritten into the relevant `findings/*` file with first-hand evidence. The seed→unit map is in the
"Notes" column below where relevant.

---

## Unit catalog (~60 units)

### Wave 1 — Engine spine (stores + core types + lib) → `findings/stores.md`, `findings/types.md`, `findings/config_lib_hygiene.md`
| Unit | Files (LOC) | Status | Findings | Notes / seed IDs to confirm |
|---|---|---|---|---|
| W1-S1 | `useCombatStore.ts` (1689) | **DONE** (S2) | stores | ✅ first-hand, all 1690 lines. 17 findings (051–067). Confirmed seed KV-AUD-025/026; +0-dmg softlock, Infinity reward, no-op buff/xp, startCombat safety gap. 6 cross-checks queued. |
| W1-S2 | `useCharacterStore.ts` (1442) | **DONE** (S3) | stores | ✅ first-hand, all 1442 lines. 12 findings (068–079). KV-AUD-068 = flat growth bypasses cost curve (THE balance root); KV-AUD-069 = resurrect-via-heal permadeath bug. Confirmed seed 023/024/026/046/047; refined 002/004/019. |
| W1-S3 | `useDungeonStore.ts` (1119) | **DONE** (S4) | stores | ✅ first-hand, all 1119 lines. 11 findings (080–090). **KV-AUD-080 = farming exploit CONFIRMED real** (re-arm + backward nav + no validation), supersedes seed 005. +unvalidated moveToNode, Date.now() seed, dead shop. Confirmed seed 021/044. |
| W1-S4a | `useSoulStore.ts` (405, est. 684 was high) | **DONE** (S5) | stores | ✅ first-hand + full cross-validation grep. 7 findings (091–097) + per-function coverage map + 30-row dual-firing table. **KV-AUD-093 = ~30 combat behavements double-counted (store + screen)** → Paragon title combat-biased. Refuted "soul dead" (it's initialized + ceremony wired). Confirmed seed 030. |
| W1-S4b | `useSacredItemStore.ts` (617) | **DONE** (S6) | stores | ✅ first-hand + item-data & caller cross-ref. 6 findings (098–103). **KV-AUD-099 (S1) sacred unlock system substantially broken** — 14 return-0 stubs + 3 no-writer metrics + dead `forceUnlock` ⇒ many of ~770 items unobtainable, `value:0` stubs give false unlocks; **KV-AUD-098** acquired/char-scope permadeath bleed. Confirmed+quantified seed 006; closed 002 sacred cross-check. |
| W1-S5a | `useShopStore`(579)+`useMarketStore`(219) | **DONE** (S7) | stores | ✅ first-hand + consumer grep. 9 findings (104–112). **KV-AUD-106** two-gold-pool disconnect (shop spends `useInventoryStore` gold, combat→`character.gold`) — resolve S5b; **KV-AUD-104** lifetimeGoldSpent bleed (closes 002 shop half); **KV-AUD-111** market wired only to guildhall materials (not dead, but mislabeled). Confirmed 016/084/064. |
| W1-S5b | `useInventoryStore`(247)+`useBlacksmithStore`(433) | **DONE** (S8) | stores | ✅ first-hand. 8 findings (113–120). **KV-AUD-106 REFUTED** (inventory is a facade over `character.gold`; no 2nd pool; corrects CLAUDE.md + refutes inventory half of 002); **KV-AUD-113 (S2) NEW silent gold-loss** (MAX_SLOTS 50 vs BAG_CAPACITY 20, addItem ignores failure); confirmed 027 (upgrade cap), 107 (dual rep), 098 (identify stale setState); **KV-AUD-118** weaponRegistry not persisted → identify/upgrade fail after reload. |
| W1-S6a | `useDeityStore`(512) | **DONE** (S9) | stores | ✅ first-hand + caller greps. 9 findings (121–129). **KV-AUD-121 (S1) deity favor mechanically INERT** — `modifyDeityFavor` has 0 callers, so `character.deityFavor` (what the blessing reads) is frozen at 50; favor changes go to `relationship.favor` and never sync. Refuted seed 004 (0× table is DEAD not a landmine) + 095 (challenges ARE wired; PROGRESS "NOT STARTED" stale). +eviction flag one-way (123), incomplete reset (124), dead startChallenge/tickChallengeFloor (125). |
| W1-S6b | `useAchievementStore`(468)+`useJobStore`(125) | **DONE** (S10) | stores | ✅ first-hand. 7 findings (130–136). **KV-AUD-130 (S2)** achievement progress gated on discovery — `hidden` achievements accrue 0 progress (real shape of seed 031). Refuted: Discovery System IS implemented (PROGRESS "NOT STARTED" stale), tier casing lowercase-consistent (040), job `as any` FIXED (052). +KV-AUD-135 job select no idempotency guard (double bonus). |
| W1-S7a | `useGameStore`(391)+`stores/index`(14) | **DONE** (S11) | stores | ✅ first-hand + caller grep. 6 findings (137–142). **KV-AUD-138 (S2)** first-combat instakill protection keyed to meta `totalRuns`/`hasHadFirstCombat` → only ever protects the FIRST character; new chars after death are unprotected. **KV-AUD-137 (S3)** deity-unlock layer (isDeityUnlocked/unlockDeity) is DEAD (0 callers) — refutes AUD-086. Confirmed 100 (barrel omits 5), resolved 096 (denatus init wired). |
| W1-S7b | `useSoundStore`(684) | **DONE** (S12) | stores | ✅ first-hand + asset Glob. 5 findings (143–147). **KV-AUD-143 REFUTES KV-AUD-007** — audio is REAL (expo-audio, real playback after stale "Would play" logs); all 53 .mp3 assets exist (no build break) → docs "audio pending" stale. **KV-AUD-144 (S2)** volume dual-source: engine volumes (0-1) non-persisted + separate from settings (0-100). ✅ **STORES WAVE COMPLETE.** |
| W1-T1 | `Stats.ts`(477)+`Character.ts`(435) | **DONE** (S13) | types | ✅ first-hand → `findings/types.md`. 14 findings (148–161). **Confirmed 5 pending seeds first-hand:** 018 grade compression (S/SS/SSS=50/30/20) +new insight grades are near-cosmetic (linear effStat); 003 dead DerivedStatFormulas (exact 10× drift); 019 cap physical-only; 041 backstory penalty no-op + non-domain affinity; 039 StatusEffect schism (Character side). +KV-AUD-153 hybrid weapons mis-scale (wrong stats). |
| W1-T2 | `Monster.ts`(566)+`Dungeon.ts`(440) | **DONE** (S14) | types | ✅ first-hand. 14 findings (162–175). **KV-AUD-162 (S2)** monster HP scales **multiplicatively** (level×8-36×zone1-9×prefix) vs **linear** player damage → deep floors likely unwinnable (probably why content stops at fl.25). Confirmed seeds 053/036 (baseCR:0→Infinity, clamp guards finalCR not divisor), 039 (schism: fear/silence add 0 CR, weaken/slow weights dead), 037 (xpValue), 038 (category:string), 022 (dead biomes), 085 (dead NODES), 044 (dead shop node). +173 three misaligned floor brackets. |
| W1-T3 | `Deity.ts`(442)+`Behavement.ts`(457) | **DONE** (S15) | types | ✅ first-hand. 9 findings (176–184). **KV-AUD-176** paragon sort proven (13/28 → 'Iron'); **177 (S2)** half noun passives dead (non-combat vectors); **180** favor tables reconciled (live FAVOR_TIERS 0.5-1.5 vs dead FAVOR_STATUS 0-2.0; live one frozen at 1.0× via 121); **181 (S2)** domain blessings display-only; 178 Mythic=100 impossible; 182 deprecated blessing helper used. Confirms seeds 011/029/020/050/004/122/128. |
| W1-T4 | `Weapon.ts`(274)+`Armor.ts`(217)+`StatusEffect.ts`(377) | **DONE** (S16) | types | ✅ first-hand. 11 findings (185–195). **KV-AUD-190 (S1)** StatusEffect schism definitive (3 models, 7/10 overlap). Inert-systems cascade: 191 statModifier dead (slow inert), 188 enchantments display-only, 193 armor mobility penalties dead, 194 non-stat-boost accessory effects dead. Confirmed 040/051/032/054/061. Refuted own 047 (no boots slot). |
| W1-T5 | `Achievement.ts`(335)+`Skill.ts`(196)+`Job.ts`(40)+`Loot.ts`(62)+`Consumable.ts`(164) | **DONE** (S17) | types | ✅ first-hand. 10 findings (196–205). **KV-AUD-196 (S2)** ~11 of 21 achievement RequirementTypes never fired → those achievements uncompletable (definitive 031/130). 199 (S2) 3 dead skill effect types; 204 (S2) reveal/identify consumables dead. Confirmed 053 (makeJobKey sort), 038/166, AUD-041/052; AUD-062 closed. |
| W1-T6 | `Shop.ts`(197)+`SacredItem.ts`(190)+`Blacksmith.ts`(166)+`PlayerSnapshot.ts`(197)+`index.ts`(22) | **DONE** | types | 206-219; sacred economy unwired (207/208→099), repair/durability inert (211), dead 0-importer barrel (217), NPC triplication (214), boss favor constant (215→121); **057 PASSES** (213) |
| W1-L1 | `clearAllStores.ts`(29)+`weaponFormulaResolver.ts`(untracked)+`sacredItemConversion.ts`(215) | **DONE** (S1) | config_lib_hygiene | ✅ first-hand. Confirmed seed KV-AUD-001 (S0 untracked) & KV-AUD-002 (reset gap); +9 new (042–050). 6 cross-checks queued: W1-S1/S2/S4/S5/T4/T6 |

### Wave 2 — Screens (`app/`) → `findings/screens.md`
| Unit | Files | Status |
|---|---|---|
| W2-P1a | `dungeon/combat.tsx` 1–1448 (logic: lifecycle/actions/soul/enemy-turn/victory/defeat) | **DONE** (S22) — KV-AUD-220–233 |
| W2-P1b | `dungeon/combat.tsx` 1448–3144 (presentation: action panel JSX, item/skill/retreat/hint modals, StyleSheet) | **DONE** (S23) — KV-AUD-234–241 |
| W2-P2 | `dungeon/room.tsx` (2036) | **DONE** (S24) — KV-AUD-242–250 |
| W2-P3 | `dungeon/floor.tsx` (1522) | **DONE** (S25) — KV-AUD-251–258 |
| W2-P4 | `dungeon/level-up.tsx` + `denatus.tsx` + `job-select.tsx` | **DONE** (S26) — KV-AUD-259–263 (★ S1 259) |
| W2-P5 | `dungeon/{encounter,travel,epitaph,boss-encounter,boss-cleared,inventory,_layout}.tsx` | **DONE** (S27) — KV-AUD-264–269 |
| W2-P6a | `town/familia/{blessing-rite,index,ascension}` (favor/ceremony core) | **DONE** (S28) — KV-AUD-270–273 |
| W2-P6b | `town/{index,_layout,character/index,inventory/index}` (hub) | **DONE** (S29) — KV-AUD-274–277 |
| W2-P7a | `town/guildhall/index.tsx` (discovery/gem-exchange/ascension-gate) | **DONE** (S30) — KV-AUD-278–280 |
| W2-P7b | `town/shops/{index,general,equipment,sell,_layout}` (buy/sell economy) | **DONE** (S32) — KV-AUD-281–285 |
| W2-P8 | `town/blacksmith/{index,repair(705),upgrade,identify,_layout}` | **DONE** (S33) — KV-AUD-286–288 |
| W2-P9 | `character-creation/**` (name,backstory,stats,confirm,equipment,deity/{index,[pantheonId],compare,_layout},_layout) | **DONE** (S34) — KV-AUD-289–290 |
| W2-P10 | `tutorial/**`(8) + `settings/*` + `codex/*` + `app/index.tsx` + `app/_layout.tsx` | **DONE** (S35) — KV-AUD-291–297 ✦ **WAVE 2 COMPLETE** |

### Wave 3 — Data & content (exhaustive + voice) → `findings/data_*.md`
| Unit | Files | Status | →file |
|---|---|---|---|
| W3-DP1…DP13 | all 19 `data/pantheons/*` (~24K LOC) | **DONE (refint)** (S50) — KV-AUD-347–351 ★ refutes 028; voice sampled (exhaustive 310-deity voice read optional) | data_pantheons |
| W3-DI1…DI5 | `data/items/*` (deityRelics*, domainArtifacts, pantheonSets/*) | **DONE** (S49) — KV-AUD-343–346 ★ discharges 099 (41% unobtainable) | data_items |
| W3-DC1 | `data/monsters/baseMonsters.ts`(1230) | **DONE** (S36) — KV-AUD-298–302 | data_combat |
| W3-DC2 | `data/monsters/{monsterArt,monsterFlavor,index}` + `data/combat/combatNarration` | **DONE** (S39) — KV-AUD-307–310 | data_combat |
| W3-DC3 | `data/weapons/*` (baseWeapons 831 + stat variants + index) | **DONE** (S40) — KV-AUD-311–315 | data_combat |
| W3-DC4 | `data/bosses/milestoneBosses`(910)+`milestoneBossesFloors30to60`(1400) | **DONE** (S41) — KV-AUD-316–320 | data_combat |
| W3-DC5 | `data/bosses/milestoneBossesFloors65to100`(1485) [`bosses/index` does not exist] | **DONE** (S42) — KV-AUD-321–324 | data_combat |
| W3-DPr1 | `data/achievements/level{1,2,3}` (+ registry-wide) | **DONE** (S43) — KV-AUD-325–328 | data_progression |
| W3-DPr2 | `data/achievements/level{4,5,6}` | **DONE** (S44) — KV-AUD-329–331 | data_progression |
| W3-DPr3 | `data/achievements/level{7,8,9}+index` | **DONE** (S45) — KV-AUD-332–334 ★ registry complete | data_progression |
| W3-DPr4 | `data/jobs/{jobDefinitions(1338),specializations,index}` | **DONE** (S46) — KV-AUD-335–339 | data_progression |
| W3-DPr5 | `data/skills/*`+`consumables`+`materials`+`loot/lootPools`+`events/dungeonEvents`(863)+`ramifications`+`weaponRegistry` | **DONE** (S47) — KV-AUD-340–342 ★ DC/DPr complete | data_progression |

### Wave 4 — Remainder + synthesis
| Unit | Scope | Status |
|---|---|---|
| W4-1 | `src/components/**` (49 .tsx) — machine pass (all 49) + sample read | **DONE** (S52) — KV-AUD-356–359 (033/008/palette discharged) |
| W4-2 | `src/constants/*` + `src/hooks/*` full re-read | **DONE** (S53) — KV-AUD-360–362 (★ GameConstants 80% dead) |
| W4-3 | config/build/deploy + root docs + repo hygiene | **DONE** (S53) — KV-AUD-363 (junk files 012); deeper root-`.md` recon → 07 |
| W4-4 | Tool sweep: `tsc`/`madge`/`knip` EXECUTED (data refint scripts done in W3) | **DONE** (S51) — KV-AUD-352–355 ★ validates 03 §6 dead-code | findings/wave4.md |
| W4-5 | Final synthesis — **06 backlog regenerated**, 02 seed finals, 00 marked AUDIT COMPLETE | **DONE** (S54) — report-only, no new IDs |

---

## Session log
- **S1 (2026-06-03):** Re-planned to multi-session resumable strategy; created this ledger + findings scaffold (`findings/config_lib_hygiene.md`). **Completed W1-L1** (lib, 3 files / ~291 LOC) first-hand: confirmed KV-AUD-001 (S0 build-break, file content verified) + KV-AUD-002 (S1 reset gap); added KV-AUD-042–050 (9 new). Next ID 051. **NEXT UP = W1-S1 `useCombatStore.ts` (1689).** Stopped here (clean checkpoint).
- **S2 (2026-06-03):** **Completed W1-S1** `useCombatStore.ts` (1690 lines, read in full) first-hand. 17 findings (KV-AUD-051–067): S2 0-damage soft-lock (poison vs bone), S2 Infinity reward on baseCR:0, S2 dead suffix-status procs, S2 inert buff/heal-% consumables, S2 `startCombat` missing first-combat protection, balance flags (hybrid double-stat, multiplicative burst, near-immortal LCK), + refined KV-AUD-003 (GameConstants is partially live — flee/luck used, crit/defend dead). Next ID 068. **NEXT UP = W1-S2 `useCharacterStore.ts` (1442).** Clean checkpoint.
- **S3 (2026-06-03):** **Completed W1-S2** `useCharacterStore.ts` (1442 lines, read in full) first-hand. 12 findings (KV-AUD-068–079): **KV-AUD-068** (S2) flat Falna growth (`commitExcelia` 1:1) bypasses the increasing `getProficiencyThreshold` curve → no per-grade cost escalation (THE balance root, confirms seed 023/AUD-087); **KV-AUD-069** (S2) `modifyHP` resurrects dead characters (permadeath bug); +inventory overflow on armor/accessory equip, triple-duplicated derived logic, unversioned persistence, dead INT/LCK growth behavements. Confirmed seeds 024/026/046/047; refined 002 (shop rep IS reset via createCharacter), 004 (live blessing uses Deity.ts 0.5–1.5 not the 0× table), 019 (Infinity cap is unarmed-only). Next ID 080. **NEXT UP = W1-S3 `useDungeonStore.ts` (1119).** Clean checkpoint. 3 units done (W1-L1/S1/S2), 38 findings logged.
- **S4 (2026-06-03):** **Completed W1-S3** `useDungeonStore.ts` (1119 lines, read in full) first-hand. 11 findings (KV-AUD-080–090): **KV-AUD-080** (S1) farming exploit **confirmed end-to-end** — `getCurrentPathOptions` returns backward connections + `moveToNode` re-arms the left node + no navigability check ⇒ re-fight cleared nodes for infinite loot (supersedes seed 005; the anti-farm irony: event-weapon cooldown exists but node re-arm doesn't); +KV-AUD-081 unvalidated moveToNode (teleport risk), KV-AUD-082 Date.now() seed + floors re-roll on ascend, dead shop node (confirms 044), dead NODES_PER_FLOOR consts (confirms 021). Next ID 091. **NEXT UP = W1-S4 `useSoulStore`+`useSacredItemStore`.** 4 units done, 49 findings. Clean checkpoint.
- **S5 (2026-06-03):** **Completed W1-S4a** `useSoulStore.ts` (405 lines) at a **deeper bar** (user feedback) — per-function coverage map, quoted code, codebase-wide behavement cross-validation grep. 7 findings (091–097): **KV-AUD-093** (S2) ~30 combat behavements fire in BOTH `useCombatStore` AND `combat.tsx` (additive `incrementBehavement`) → combat vectors inflate 2× → Paragon title biased (30-row store→screen table); **KV-AUD-091** 90 behavements not 85; **KV-AUD-092** non-atomic increment race; **KV-AUD-094** unversioned 90-elem persist array breaks on defs change; **KV-AUD-095** explore_secret_rooms/glory_challenge_complete fire but trigger maybe unreachable. **Refuted** the "soul system dead" worry — `initializeDenatus` IS called (`useGameStore:340,347`) + ceremony wired (`denatus.tsx:117`); **corrected** W1-S1 KV-AUD-067 (store firing is over-wiring, not just "good"). Split W1-S4→S4a(done)/S4b(todo). Next ID 098. **NEXT UP = W1-S4b `useSacredItemStore` (617).** 5 units done, 56 findings.
- **S6 (2026-06-03):** **Completed W1-S4b** `useSacredItemStore.ts` (617 lines) at the deep bar + two cross-reference greps (item data + metric callers). 6 findings (098–103): **KV-AUD-099 (S1)** the sacred-item unlock system is substantially broken — 14 `return 0` metric stubs + `recordParagon`/`incrementEventSuccess` never called + no `incrementSkillUse` + **`forceUnlock` is dead code (zero callers)** ⇒ a large share of ~770 items (esp. `level_reached`/`stats_grade` single_character relics: `deityRelics_dark.ts:31,49,80,204`, `eastAsian.ts:219,298,313`…) are **unobtainable**, while `value:0` "must-be-zero" requirements on dead metrics give **false unlocks** (`eastAsian.ts:50` boss_noattack); **KV-AUD-098 (S2)** `acquired`+char-scope never reset ⇒ items earned by a dead character are permanently lost to new characters (skipped via `acquired.includes`). **KV-AUD-100** inconsistent checkAndUnlock (recordMaxFavor doesn't trigger secret-item check); **KV-AUD-101** O(770)-per-tick unlock scan. Confirmed+quantified seed 006; closed the 002 sacred cross-check. Next ID 104. **NEXT UP = W1-S5 economy (4 stores, may split).** 6 units done, 62 findings.
- **S7 (2026-06-03):** **Completed W1-S5a** `useShopStore`(579)+`useMarketStore`(219) first-hand + market-consumer grep. 9 findings (104–112): **KV-AUD-106 (S2)** the shop spends `useInventoryStore` gold while combat rewards go to `character.gold` → possible core-economy disconnect (TOP thread for S5b); **KV-AUD-104 (S2)** shop `lifetimeGoldSpent` bleeds (resetReputation resets rep only) — closes the shop half of seed 002; **KV-AUD-105** stock tier keyed to cross-character `bestFloorReached`; **KV-AUD-111** the "Living Economy" market is wired ONLY to guildhall material sales (not dead — corrected hypothesis — but mislabeled); **KV-AUD-107** dead dual blacksmith-rep. Confirmed 016/084/064; market `reset()` IS in clearAllStores (no bleed). Split W1-S5→S5a(done)/S5b(todo). Next ID 113. **NEXT UP = W1-S5b `useInventoryStore`+`useBlacksmithStore`.** 7 units done, 71 findings.
- **S8 (2026-06-03):** **Completed W1-S5b** `useInventoryStore`(247)+`useBlacksmithStore`(433) first-hand. 8 findings (113–120). **KV-AUD-106 REFUTED** — `useInventoryStore` is a facade over `character.gold`/`character.inventory` (no 2nd pool); dissolves the W1-S5a top thread, refutes the inventory half of seed 002, and corrects CLAUDE.md's "separate stash" claim. **NEW KV-AUD-113 (S2)** silent gold loss: `MAX_INVENTORY_SLOTS=50` vs `BAG_CAPACITY=20`, `addItem` returns true even when `addToInventory` rejects → buying at 20–49 items charges gold but adds nothing (compounds KV-AUD-070). **Confirmed** KV-AUD-027/119 (upgrade omits maxOutputCap), 107 (blacksmith rep canonical in useBlacksmithStore; shop's is dead), 098/114 (identify stale-snapshot setState). **KV-AUD-118 (S2)** weaponRegistry is in-memory/not persisted → identify/upgrade fail after app reload (actions don't self-heal, list helpers do). Blacksmith `reset()` IS in clearAllStores (no bleed). **W1-S5 economy bundle complete.** Next ID 121. **NEXT UP = W1-S6 deity+achievement+job.** 8 units done, 79 findings.
- **S9 (2026-06-03):** **Completed W1-S6a** `useDeityStore`(512) first-hand + favor/challenge caller greps. 9 findings (121–129). **KV-AUD-121 (S1, the audit's 3rd S1)** deity favor is mechanically inert for combat power: `.modifyDeityFavor(` has **0 callers** → `character.deityFavor` (read by the blessing multiplier) is frozen at 50, while all favor changes (shrine/domain/challenge) update only `relationship.favor` and never sync → raising favor does nothing to HP/SP/combat (falsifies "deity favor directly impacts power"). **Refuted** seed 004 (FAVOR_STATUS 0× multiplier is DEAD — only consumer is a discarded self-call `:474` — not a landmine; downgraded to S3) and seed 095 (God Challenges ARE wired: `issueChallenge`←familia:236, recordChallengeEvent←room/combat/floor → PROGRESS "NOT STARTED" is stale; glory_challenge_complete reachable). +KV-AUD-123 eviction flag never cleared on recovery, 124 reset incomplete, 125 dead startChallenge/tickChallengeFloor, 126 challenge reward STR/END/AGI/PER only. Split W1-S6→S6a(done)/S6b(todo). Next ID 130. **NEXT UP = W1-S6b achievement+job.** 9 units done, 88 findings.
- **S10 (2026-06-03):** **Completed W1-S6b** `useAchievementStore`(468)+`useJobStore`(125) first-hand. 7 findings (130–136). **KV-AUD-130 (S2)** achievement progress is gated on discovery state — `updateProgress`/`incrementProgress` skip `discoveryState==='hidden'` (`:106,152`), so hard-to-discover tiers accrue zero progress even when earned (the real shape of seed 031; STANDARD tiers auto-unlock so leveling isn't blocked). +KV-AUD-132 O(123)-per-event scan, 135 job-select no idempotency guard (double stat bonus + dup skill). **Refuted 3 more stale claims:** Discovery System IS implemented (PROGRESS "Phase 2.3 NOT STARTED" stale — same as challenges in S9), tier casing lowercase-consistent (refutes 040), job `as any` is FIXED (refutes 052). **Meta-pattern: PROGRESS.md systematically UNDERstates Phase-2 completion while CLAUDE.md OVERstates the stack — docs unreliable in both directions** (→ 07_DOC_RECONCILIATION). Next ID 137. **NEXT UP = W1-S7 game+sound (LAST stores unit).** 10 units done, 95 findings.
- **S11 (2026-06-03):** **Completed W1-S7a** `useGameStore`(391)+`stores/index`(14) first-hand + deity-unlock caller grep. 6 findings (137–142). **KV-AUD-138 (S2)** the first-combat instakill safeguard (`prepareEncounter:309` `totalRuns===0 && !hasHadFirstCombat`) keys off META flags that persist across characters and aren't reset on new game → only the literal first-ever character is protected; every new character after a death can be handed a boss/elite on fight #1. **KV-AUD-137 (S3)** the deity-unlock meta-layer (`isDeityUnlocked`/`unlockDeity`/`unlockedDeities`) is **dead code — 0 callers** → refutes prior AUD-086 ("97% of deities unreachable"); they're all selectable, the lock is just unwired. +139 totalDeaths over-counts, 140 barrel omits 5 stores (confirms 100), 141 settings no-migration. **Resolved KV-AUD-096** (initializeDenatus wired via tutorial + confirm.tsx). Split W1-S7→S7a(done)/S7b(todo). **14 of 15 stores audited.** Next ID 143. **NEXT UP = W1-S7b useSoundStore (last stores unit).** 11 units done, 101 findings.
- **S12 (2026-06-03):** **Completed W1-S7b** `useSoundStore`(685) first-hand + asset Glob. 5 findings (143–147). **KV-AUD-143 (refutes KV-AUD-007 + components-agent XC-2 + PROGRESS/CLAUDE):** audio is **fully implemented** (expo-audio: real `createAudioPlayer`+`play` after each stale `Would play` log; preload cache, crossfade, fades), and a Glob confirms **all 53 referenced `.mp3` assets exist** → no build break, docs "audio pending Phase 3" are STALE. **KV-AUD-144 (S2)** volume dual-source: `useSoundStore` volumes (0–1) are **non-persisted** + separate from `useGameStore.settings` (0–100, persisted); the engine reads the non-persisted ones → audio volume never persists / may not reflect Settings (closes seed 015). +146 seven placeholder BGM tracks (town/6 blessing domains reuse existing files). **✅ STORES WAVE COMPLETE (12 store/lib units, KV-AUD-001–147).** Next ID 148. **NEXT UP = W1-T1 Stats+Character (types wave begins → `findings/types.md`).** 12 units done, 106 findings.
- **(Session: housekeeping)** Created `AUDIT_PLAN.md` (consolidated plan) + `ALL_FINDINGS.md` (all 147 findings + cross-finding reconciliation: 18 earlier findings affected — 6 refuted, 2 downgraded incl. 004/007, 4 refined incl. 002/003, 10 superseded, 1 dup 029≡011). Owner chose to **defer** the 00/02/03/05/07 rewrites to end-of-Wave-1 synthesis.
- **S13 (2026-06-03):** **Completed W1-T1** `Stats.ts`(477)+`Character.ts`(435) first-hand → new `findings/types.md`. 14 findings (148–161). **Discharged 5 pending seed cross-checks with exact evidence:** **KV-AUD-150** confirms 018 (grades S/SS/SSS=50/30/20 wide; A→SSS=199 pts vs I→A=800) + new insight: combat reads linear effStat so grades are **near-cosmetic**; **151** confirms 003 (Stats.ts hardcodes coeffs, 10× drift on atk/speed/crit, HP matches); **152** confirms 019 (cap physical-only; magic/luck uncapped); **156/157** confirm 041 (backstory penalty applied nowhere → pure upside; deityAffinity 'divine'/'fate' non-domain); **158** confirms 039 (StatusEffect schism, Character side — fear/silence/paralysis dead). +**KV-AUD-153 (S2)** hybrid weapons mis-scale (fallback hardcodes effSTR/effINT, ignoring declared stats); +148 applySoftCap dead, 149 STAT_INFO phantom mechanics, 154 dead prof curve, 159 getCarryCapacity dead. Balance synthesis: **068×150×003 = prestige tiers that are cheap, marginal, and untunable.** Next ID 162. **NEXT UP = W1-T2 Monster+Dungeon.** 13 units done, 120 first-hand findings (+41 seeds).
- **S14 (2026-06-03):** **Completed W1-T2** `Monster.ts`(566)+`Dungeon.ts`(440) first-hand. 14 findings (162–175). **KV-AUD-162 (S2, headline)** deep floors likely **mathematically unwinnable** — monster HP stacks 3 multiplicative layers (`monsterLevel × levelMultiplier 8-36 × zoneMultiplier 1-9 × prefix`) while player damage is linear (`effStat×~0.008`); at fl.86 a CR-11 monster ≈ 28-55k HP vs ~tens/hit → hundreds of hits, and monster attack one-shots the player. Structural mismatch certain; magnitude needs a 100-floor sim. Likely explains why content stops at floor 25. **Confirmed 7 dead/schism seeds first-hand:** 053/036 (calculateMonsterCR clamps finalCR≥0.1 but reward divides by raw base.baseCR → Infinity on baseCR:0), 039 (STATUS_EFFECT_CR_WEIGHTS uses weaken/slow but suffix.statusEffect is fear/silence/paralysis → 0 CR / dead weights), 037 xpValue, 038 category:string, 022 dead void/labyrinth biomes, 085 dead NODES consts, 044 dead shop node. +KV-AUD-168 SUFFIX_FLOOR_GATES guard a mechanism that never fires (052), 173 three misaligned floor-bracket schemes, 174 dup danger fns. Next ID 176. **NEXT UP = W1-T3 Deity+Behavement.** 14 units done, 134 first-hand findings (+41 seeds).
- **S15 (2026-06-03):** **Completed W1-T3** `Behavement.ts`(457)+`Deity.ts`(442) first-hand. 9 findings (176–184). **KV-AUD-176** Paragon sort bug **proven exactly**: 13 of 28 stat pairs → 'Iron' (alphabetical sort vs non-alpha map keys; all STR_* except STR_WIS, END_AGI/CHA, INT_CHA, WIS_*, PER_*). **KV-AUD-177 (S2, NEW)** half the Paragon noun passives are **dead** — CAUTION/SOCIAL/EXPLORATION/RESOURCE/GLORY effect types (item_effectiveness/reputation_gain/gold_bonus/loot_discovery/tier_bonus) aren't applied by getDerivedStatsWithBlessings. **KV-AUD-180** favor tables definitively reconciled: live FAVOR_TIERS (0.5-1.5, Abandoned 0.5×) vs dead FAVOR_STATUS (0-2.0, 0×) — and the live one only ever returns 1.0× because favor frozen at 50 (121). **KV-AUD-181 (S2)** deity DOMAIN blessings (+10% etc.) appear display-only/not applied. **178** Mythic=100 literally impossible (dead behavements cap score). +182 deprecated calculateBlessingPower still used, 183 checkDeityApproval opposition map 4/14. Reward systems = richest "looks complete, isn't wired" vein. Next ID 185. **NEXT UP = W1-T4 Weapon+Armor+StatusEffect.** 15 units done, 143 first-hand findings (+41 seeds).
- **S16 (2026-06-03):** **Completed W1-T4** `Weapon.ts`(274)+`StatusEffect.ts`(377)+`Armor.ts`(217) first-hand. 11 findings (185–195). **KV-AUD-190 (S1, definitive)** the StatusEffect schism — THREE incompatible status models (StatusEffect.ts weaken/slow/regen [combat]; Character.ts fear/silence/paralysis [persisted/consumables]; +'buff' shape); 7/10 IDs overlap → fear/silence/paralysis dead, weaken/slow CR-weights dead, cure-by-id fails. **Cascade of inert equipment systems:** KV-AUD-191 (S2) `statModifier` dead → **slow fully inert**; 188 (S2) weapon enchantments likely display-only; 193 (S2) armor speed/dodge penalties dead; 194 (S2) non-stat-boost accessory effects dead. Confirmed seeds 040 (hybrid set: CHA_INT vs INT_CHA + dead INT_PER/WIS_PER/WIS_LCK), 051/032 (poison vs bone/armor/spirit = 0 dmg softlock), 054, 061. **Refuted my own KV-AUD-047** (only 4 armor slots, no boots → getArmorStatBonuses misses nothing). Synthesis: **Kohrvellia models far more than it wires.** Next ID 196. **NEXT UP = W1-T5 Achievement+Skill+Job+Loot+Consumable.** 16 units done, 154 first-hand findings (+41 seeds).
- **S17 (2026-06-04):** **Completed W1-T5** `Achievement.ts`(335)+`Skill.ts`(196)+`Job.ts`(40)+`Loot.ts`(62)+`Consumable.ts`(164) first-hand. 10 findings (196–205). **KV-AUD-196 (S2, definitive 031/130):** **~11 of 21 achievement RequirementTypes are never fired** (kill_type, floor_return, no_damage, low_hp_win, item_collect, reputation, skill_use, status_inflict, stealth_kills, flee_success, no_consumables) → achievements using them **uncompletable**. More inert-systems: **199 (S2)** 3 dead skill effect types (damage_percent/buff/flee in playerUseSkill); **204 (S2)** reveal/identify consumables do nothing. Confirmed 053 (makeJobKey alphabetical-sort trap, same class as 176), 038/166 (MonsterCategory union vs Monster.category:string), AUD-041 (getTierSynergyBonus dead), AUD-052 (two skill types), AUD-062 CLOSED (single sell-price path). Next ID 206. **NEXT UP = W1-T6 (LAST W1 type unit) → then WAVE 1 COMPLETE → synthesis.** 17 units done, 164 first-hand findings (+41 seeds).
- **S18 (2026-06-04):** **Completed W1-T6** `Shop.ts`(197)+`SacredItem.ts`(190)+`Blacksmith.ts`(166)+`PlayerSnapshot.ts`(197)+`index.ts`(22) first-hand + 9 cross-check greps. 14 findings (206–219). **Two subsystems confirmed unwired at the engine level:** the **sacred-item economy** — KV-AUD-207 (50 metrics × 4 scopes ≫ the evaluator), **208** (`revealFavorRequired` read by **0 stores** + double-locked by the 121 favor freeze → deity relics at 100 never even reveal), 210 (passives decorative) — all feeding the S1 **KV-AUD-099**; and the **blacksmith repair** service — **KV-AUD-211** (`Weapon.durability` has **0 store reads/writes** → never degrades → `repair.tsx` is a no-op; section even labeled "REPAIR SYSTEM (Future)"). +**KV-AUD-215** the boss deity-favor sense is **constant** ('neutral'/50 always, downstream of 121) and a 3rd favor scheme (extends 180); **217** the types barrel is **dead (0 importers) AND incomplete** (9/18 modules → escalates 056); 214 blacksmith NPC **triplicated/divergent**; 216 redundant+mislabeled boss flags; 206 sacred `damageType` divergence (`'physical'`/`'chaos'` ∉ canonical `DamageType`); 218 `as unknown as` contract bypass. **Counter-evidence recorded:** seed **057 referential integrity PASSES** (213 — all 7 upgrade materials exist; price tables match canonical tiers) and the **archetype map is sound** (REFUTED my own "always Berserker" hypothesis — `WeaponCategory` IS the 8-stat set). Resolved seeds 054/055/056/057/058/060/061. Next ID 220. **★ WAVE 1 COMPLETE — engine spine (stores+types+lib): 18 units, KV-AUD-001..219, 178 first-hand findings (+41 seeds). NEXT UP = deferred Wave-1 synthesis (regenerate 00/02/03/05/07 from `ALL_FINDINGS.md`).**
- **S19 (2026-06-04) — Wave-1 synthesis, part 1 (3 of 5 docs):** Brought **`ALL_FINDINGS.md`** current through 219 — extended the register with the full types wave (148–219), updated §3 tally to the post-types reality (**5 S1s**: 002/080/099/121/**190**; ~59 S2; ~70 S3), added reconciliation §1G (types wave: new S1 190, ~20 seeds confirmed, self-corrections, compounding cross-links), and added the one-line thesis. Regenerated **`00_EXECUTIVE_SUMMARY.md`** from scratch — corrected the scorecard (was wrong: listed 7 S1s incl. refuted 007 + downgraded 004; now the real 5), added the five-S1 table + the "models more than it wires" thesis + an honest Wave-1-complete/Waves-2–4-pending coverage table; dropped the obsolete "interrupted agent passes" framing. Regenerated **`07_DOC_RECONCILIATION.md`** — corrected the audio row (fully implemented, refutes the stub claim), the behavement count, added the **meta-pattern** (PROGRESS understates / CLAUDE.md overstates), flagged the weapon-triangle dispute, and rebuilt the AUD→KV crosswalk with superseded IDs + escalations (039→190 S1). **Remaining: 02 (seed annotations), 03 (dead-code inventory), 05 (balance) — see RESUME POINTER.** Stopped here for a clean checkpoint (context discipline; no mid-write risk).
- **S20 (2026-06-04) — Wave-1 synthesis, part 2 (COMPLETE):** Regenerated the final 3 rollups from `ALL_FINDINGS.md`. **`02_FINDINGS.md`** — annotated all 41 seeds with disposition (Confirmed / Refined / Downgraded / Superseded→### / Refuted / Pending-wave) + a legend; merged 029→011; re-verified 001 (still untracked) and 012 (junk files still present) against the live tree. **`03_CONNECTIVITY_AND_DEADCODE.md`** — added orphans 137 (dead deity-unlock layer, refutes AUD-086), 099 (dead `forceUnlock`), 125 (dead startChallenge/tickChallengeFloor), 122 (dead 0× `FAVOR_STATUS`), 217 (dead+incomplete types barrel); added no-ops 121 (favor inert), 181 (domain blessings display-only), 188/191/193/194 (enchant/slow/armor/accessory inert), 211 (durability/repair no-op), 052/055 (suffix/xp); **removed** the audio-stub ASPIRATIONAL entry (refuted by 143) and the discovery/challenges "NOT STARTED" entry (refuted by 127/134); promoted confirmed orphans (085/148/159/169/172) and moved `calculateBlessingPower` out of ORPHAN (182: deprecated-but-still-called). **`05_BALANCE_AND_BENCHMARKS.md`** — promoted B1–B4 to Confirmed (151/068/150/152); **rewrote B5 around 121 (favor inert, S1)** and downgraded the old 0×–2.0× death-spiral (004→S3 via 122; +180 divergent tables, +215 boss-const, +181 display-only); added §2 deep-floor scaling (162, the headline — mechanism confirmed, 100-floor sim still owed), §4 hybrid mis-scale (153/185/061/062), and economy updates (211 repair-noop sink, 113 gold-loss, 107 dup rep); reprioritized the tuning recommendations. No new findings (report regeneration only) → Next ID unchanged at **KV-AUD-220**. **★ WAVE 1 FULLY COMPLETE (engine spine + synthesis). NEXT UP = WAVE 2 screens @ `app/dungeon/combat.tsx`.** Clean checkpoint.
- **S21 (2026-06-04) — Wave-1 consistency & expansion pass (report-only; no new findings, no new IDs — Next ID stays KV-AUD-220):** Re-read all three `findings/*.md` in full and cross-checked every first-hand finding against later ones for contradictions / withdrawn claims / now-resolvable "verify later" threads. **Propagated 9 within-Wave-1 self-corrections back into the source prose** as *additive* `[Update S21]` annotations (nothing deleted — append-only preserved): **067**→096 (well-wired ⇒ over-wiring/093 double-count), **047**→195 (**WITHDRAWN** — only 4 armor slots exist, nothing omitted), **106** S5a→S5b (**REFUTED** two-gold-pool — facade over `character.gold`), **024**→121 (mooted — favor frozen ⇒ blessing ~1.0×), **058**→138 (compounded — first-combat guard dead for char #2+), **095**→127/178 (split — `glory_challenge_complete` reachable, `explore_secret_rooms` confirmed unreachable → caps Mythic <100), **046** (discharged — bonuses live, type-debt only), **057**→116/199/204 (dead-effect-handler cluster), **077**→152 (unarmed `Infinity` cap confirmed). **Consolidated into a new `ALL_FINDINGS.md §1H`** (within-Wave-1 self-corrections table + method note) and **added 3 compound cross-links to §1F** (hybrid-weapon 4-way `043×185×153×061`; vestigial-XP `055×165×203`; dead-effect-handler `057×199×204`); marked 047 WITHDRAWN in the §4 register and softened the W1-S5a verdict. **No severity/count changes** — 5 S1 / 1 S0 stand (the 047 withdrawal is one approximate S2). The Wave-1 corpus is now internally self-consistent. **NEXT UP unchanged = WAVE 2 screens @ `app/dungeon/combat.tsx`.**
- **S22 (2026-06-04) — ★ WAVE 2 BEGINS: W2-P1a `combat.tsx` lines 1–1448 (logic half), first-hand:** the most-connected screen (reads 6 stores). 14 findings (**KV-AUD-220–233**) → new `findings/screens.md`. **Discharged the queued Wave-1 cross-checks, several reversing assumptions:** **093** double-count **CONFIRMED per-event** (220 — the screen fires the same behavement IDs the store actions fire, traced handler-by-handler; combat kills also inflate EXPLORATION); **055/164/165/203** "xp vestigial" **REVISED→live** (222 — `rewards.xp` IS consumed as proficiency at `:1020`, but distributed to STR/END/AGI/PER **only** → INT/WIS/CHA/LCK builds gain nothing, routed through the flat-growth path 068); **177** "gold_bonus never applied" **PARTIALLY REFUTED** (223 — it's screen-wired in `handleVictory:999`, just not in the derived path); **024** "blessed selector dropped" **REFUTED** (225 — combat aliases `getDerivedStatsWithBlessings` at `:139`); **056** rewards race **CONFIRMED SAFE** but fragile (226); **057/116** buff (and reveal/identify) consumables **CONFIRMED inert in combat** (224). **New issues:** **permadeath not committed until "Accept Fate" tapped** → force-close on the FALLEN screen = zombie `currentHP≤0/!isDead` character (227, S2, R10, compounds 069/BUG-001); magic kills increment `phys_consecutive_kills` (221, S2); four per-floor behavements fire **only on boss floors** (232, S3); kill-and-die → DEFEAT voids the win (231, latent S3→S1 if self-damage added); unselected full-store subscriptions re-render the whole screen (230, S2 perf); screen-side balance literals (229). Positives (233): DoT-death pre-calc dodges the batched-state race, `registerWeapon` present in all pickups (BUG-035 fix), `handleDefeat` snapshots before clearing. Next ID **234**. **➡ NEXT UP = W2-P1b** (`combat.tsx` 1448–3144, presentation half).
- **S23 (2026-06-04) — W2-P1b `combat.tsx` 1448–3144 (presentation half), first-hand → ★ `combat.tsx` COMPLETE:** 8 findings (**KV-AUD-234–241**). **234 (S2)** the retreat-confirmation modal calls `playerFlee` directly (`:1849`), bypassing `runFlee` — wrong speed formula, no `markNodeAvoided`/`router.back` on success (and there's **no `'fled'` render guard** → falls through to the disabled combat UI; node re-arms per 080), no AGI/behavement tracking, and no enemy turn on failure; a buggy duplicate flee path (Android hardware-back reachable). **235 (S3)** combat-log length is **5 (display, `:1472`) vs 20 (store) vs 8 (docs)** — refines 064. **236 (S3)** hardcoded hex off the token system — the **entire retreat modal** is bespoke (`#1E1214/#C84832/#E8D8C0/#A08870`, `:3039-3088`) + `#00BFFF` teal ×5 + `#9B59B6`; confirms the 035 pattern first-hand. **237 (S3)** the StyleSheet is a **~50-entry dead-style museum** (pre-component enemy/log/player block `:1927-2096`; "legacy — kept for possible reuse" action buttons `:2098-2152`; explicit "backward compat (unused after J4 rename)" `invokeKairosBtn`/`kairosSwift`/`phasePlayer`/`stagedBonusHint`) — the GameConstants-museum pattern (003) in CSS. **238 (S3)** inline-closure BONUS-row array rebuilt every render × unselected store subscriptions (230). **239 (S3 / Needs-W3)** skills staged with raw grade `.points` (0-999, `:722,1778`), not effStat → likely mis-scales vs attacks. **240 (S3)** status-badge color/icon lookups have no fallback (latent via the 190 schism). Positives (241): both modals use `FlatList`, backdrop `stopPropagation`, first-run hint gate, `canUseSkill` gating. **★ `combat.tsx` FULLY AUDITED 1–3144 across P1a+P1b: 22 findings (KV-AUD-220–241).** Next ID **242**. **➡ NEXT UP = W2-P2 `app/dungeon/room.tsx` (2036 LOC).**
- **S24 (2026-06-04) — W2-P2 `app/dungeon/room.tsx` (2036, full file), first-hand:** 9 findings (**KV-AUD-242–250**). **★ HEADLINE — an S1 reversal: KV-AUD-242 (S2) downgrades the S1 KV-AUD-121.** `modifyDeityFavor` IS called — `room.tsx:71,497` destructures it and calls `modifyDeityFavor(favorDelta)` at the shrine (alongside `adjustFavor` `:498`); the W1-S6a "0 callers" grep (`.modifyDeityFavor(`, leading dot) was a **false negative** — it missed the destructured bare call. So `character.deityFavor` is NOT frozen at 50: shrine favor moves it + recomputes the blessing. 121 reframed → favor is **inconsistently synced** (shrines update both stores; events/challenges only `relationship.favor`), not inert ⇒ **S1→S2, audit S1 count 5→4** (002/080/099/190 remain). **Propagated** to `stores.md` (121+095 annotations), `ALL_FINDINGS` (§3 tally + §4 register), `00_EXECUTIVE_SUMMARY` (scorecard + S1 table + health row), `05` (B5). **243 (S2)** shrine "opposed" affinity NEVER fires — `opposites` map is Capitalized but `deity.domain` is lowercase (`:432-440`) → BUG-042's "fix" is dead on a casing bug. **244 (S3, revises 095/178)** `explore_secret_rooms` IS reachable (fires on mystery reveal `:772`; mystery nodes generate) — corrects my own S21 note. **245 (S3, quantifies 083/172)** the full dungeon-shop (render+handlers+2 effects+styles, `:1322-1358`) is dead code (generator excludes shop nodes). **246 (S2)** reward nodes re-farmable (reward banks on action; `completeNode` only on "Continue"; the always-present "Return to Map" `:1566` permits leaving incomplete → re-loot) + **no `BackHandler` (`:7`)** so hardware/gesture back bypasses the No-Retreat gate (compounds 080). **247 (S3)** inline balance + hardcoded-hex literals; **248 (S3)** shrine blood-offering can silently kill (no death handling, cross-link 069/227); **249 (S3)** mystery handles more reveal types than the generator produces. Positives (**250**): BUG-036 fixed (resultTier), **BUG-039 syncs both favor stores — the 242 evidence**, `recordChallengeEvent`/discovery/sacred-metrics all wired. Next ID **251**. **➡ NEXT UP = W2-P3 `app/dungeon/floor.tsx` (1522 LOC).**
- **S25 (2026-06-04) — W2-P3 `app/dungeon/floor.tsx` (1522, full file), first-hand:** 8 findings (**KV-AUD-251–258**). The map screen **closes the 080 S1 farming loop at the player surface**: **251 (S2)** `getCurrentPathOptions` (backward-inclusive, W1-S3) drives the pulsing GO badge + tooltip "Go Here" + node press → `moveToNode` with **no directionality guard**, so the player can tap the re-armed node they came from and re-fight (the connection renderer only draws downward → a backward GO badge can show with no visible line). **252 (S2)** confirms 082 — `handleAscendFloor`'s Alert literally says "A new path will be generated" → ascend re-rolls fresh content. **253 (S3)** confirms 232 — `handleDescend` fires floor_reach/explore_floors/glory_no_death/challenge events but NOT the per-floor-conduct behavements (magic_no_physical/risk_no_heal_floor/caution_no_traps/glory_perfect_floor), which fire only on boss kills → dead on ~95 floors. **254 (S3)** "Menu" button (`:794`) escapes to title mid-run with no `endRun` (dangling currentRun) + famine `modifyHP(-8)` can silently kill. **255 (S3)** hardcoded hex + the off-token "second palette" recurring across combat/room/floor + dead-biome gradients (void/dreamscape/labyrinth, 169). **256 (S3)** map-render perf (per-connection trig + inline closures + unselected subscriptions) + dead/empty styles. Positives: **257** discharges 086 (`enterFloor` is descend-gated at `:312`, not on mount) + confirms 218/026 (`:415` as-any cast); **258** BUG-023 boss-cleared reachable (`:427`), challenges wired (`:324-326`), native-driver pulse, fog-of-war. No severity changes (080 stays S1). Next ID **259**. **➡ NEXT UP = W2-P4 `level-up.tsx`+`denatus.tsx`+`job-select.tsx`.**
- **S26 (2026-06-04) — W2-P4 `level-up.tsx`+`denatus.tsx`+`job-select.tsx`, first-hand (+`ascension.tsx:1-230` cross-ref):** 5 findings (**KV-AUD-259–263**). **★ NEW S1 — KV-AUD-259: the Level-10 Denatus/Paragon climax is UNREACHABLE.** Routing audit (grep across `app/`): **nothing routes to `/dungeon/level-up`** (superseded by `town/familia/ascension.tsx`, routed from `familia:339`); `/dungeon/denatus` is routed ONLY from the dead `level-up.tsx:309`; the live `ascension.tsx` `handleComplete` (`:194-210`) goes to town (or job-select at L5), **never denatus**, and never calls `performDenatus`/`setParagonTitle` (both exist only in denatus.tsx). ⇒ **`character.paragonTitle` is ALWAYS null** ⇒ the entire Paragon buff layer (combat `gold_bonus` 223, the derived paragon branch, 177 passives) + the soul-climax (011/176/178/093/097/179) is dead in practice. **Audit S1 count 4→5** (002/080/099/190/259 — a different set than Wave-1's after 121-down + 259-up); propagated to `00` (scorecard + S1 table + health row) and `ALL_FINDINGS` (§3 tally + prose). **260 (S2)** `level-up.tsx` is dead (855 LOC, no inbound route; confirms 009 deity-name no-op `:354`, 027/133 GLORY wiring, BUG-004 guards — all in dead code). **261 (S2, latent behind 259)** denatus defects when revived: `getTopTwoStats` ranks by raw `.points` ignoring grade + no agency (097/179), title generated on mount but applied only in `handleFinish` → early exit strands it, `recordParagon`/`forceUnlock` never called (099), displays 093-biased vectors + unreachable Mythic (178). **262 (S3)** `job-select.tsx` half-reachable — base-job mode dead (only from dead `level-up:315`; L2 jobs done inline in ascension), spec mode live (`ascension:204`); store idempotency (135) mitigated by screen+routing; uses effStat (correct) unlike denatus's raw points. Positives (263): ascension (live) has BackHandler + favor dialogue + weapon gift; combat's paragon-READ side is correct — it's the WRITE side (denatus) that's unreachable. Next ID **264**. **➡ NEXT UP = W2-P5 (encounter/travel/epitaph/boss-*/inventory/_layout).**
- **S27 (2026-06-04) — W2-P5 dungeon bundle (encounter 335 / travel 521 / epitaph 275 / boss-encounter 403 / boss-cleared 98 / inventory 750 / _layout 39), first-hand:** 6 findings (**KV-AUD-264–269**). **264 (S2)** `_layout.tsx` Stack sets **no `gestureEnabled:false`** → iOS edge-swipe-back is live on every dungeon screen; combat (234) + room (246) guard only Android hardware-back (`BackHandler`), so iOS players can swipe out of active combat / unresolved+reward nodes — the **root enabler** of the 234/246/254 nav-integrity cluster (only level-up/job-select/ascension add a `beforeRemove` guard). **265 (S2)** milestone-boss combat entry is fragile — `boss-encounter.startCombatAndGo` passes the store's `monster`, but floor→boss-encounter calls only `setBossSnapshot` (no `prepareEncounter`), so `monster` is null post-`endCombat` → the FIGHT button falls to `router.replace('/dungeon/room')` (`:206`) → room "Challenge Boss"→`prepareEncounter`→encounter→combat (2-screen detour; run-flags survive via `setRunFlag`). **266 (Info)** BUG-022/034 RESOLVED — `executeOutcome` reads `resolvedOutcome.achievement` + generics, discover-then-progress; revises 215 (boss favor-sense varies now, via 242). **267 (S3)** inventory: weapon-only grid + armor "future update 🔒" placeholder (BUG-031), cure-consumables cosmetic (no `removeStatusEffect`, `:74-76`), enchantments displayed-not-applied (188). **268 (S3)** entry/exit cluster: 058 confirmed (`startCombat` dead; live entry = `startCombatWithMonster`; 138 protection upstream), navigate-during-render anti-pattern (boss-cleared:29, boss-encounter:54), travel drops status/buff ramifications (`:104`) + silent-death risk, off-token hex. Positives (269): encounter sneak `markNodeAvoided` (BUG-021), travel double-apply guard, boss-cleared (BUG-023), epitaph snapshot flow, strong boss-conversation content. No new S1s (count stays 5). Next ID **270**. **➡ NEXT UP = W2-P6 town hub + familia (Blessing Rite — completes the 121/242 favor picture; full `ascension.tsx`).**
- **S28 (2026-06-04) — W2-P6a `town/familia/{blessing-rite,index,ascension}`, first-hand (logic + favor cross-checks; render/style tails skimmed):** 4 findings (**KV-AUD-270–273**). **★ KV-AUD-270 (S2) — the favor→power picture is COMPLETE: shrine-only.** Grep `town/familia/*` for `modifyDeityFavor|adjustFavor` = **0 hits** — blessing-rite commits excelia (`:186`, the flat-growth 068 path) + shows a growth-tiered deity message but **never touches favor**; familia displays `relationship.favor` + runs challenges/eviction but **never syncs to `character.deityFavor`**; ascension likewise. So the entire intended patron loop is power-inert; only `room.tsx:497` (shrine) moves the power-relevant favor. **Completes/sharpens 242** (favor inconsistently-synced → the sync is shrine-only; the main loop is dead). 122 refuted: `familia:306` displays `FAVOR_STATUS.label`, not the dead `.multiplier`. **271 (S3)** ascension's `blessing` phase hardcodes `<Text>LEVEL 2</Text>` (`:278`) → every level-up shows "LEVEL 2"; phase set is L2-shaped with no Paragon/denatus phase → on-screen reconfirmation of 259. **272 (S3)** blessing-rite minors: dead `oldValue/newValue` reveal fields (`:202` "rough estimate", not rendered), phantom `DOMAIN_GLYPH` domains (`:52-56`, water/earth/shadow/chaos/order/void ∉ the 14), `commitExcelia` = the 068 flat-growth manifest, Falna hint formula omits the carry term (`:517`). Positives (273): eviction Option C wired (`:147-164`), full challenge flow (issue/abandon/reward, confirms 127), relic-reveal UI (`:130-145`, partial 208 refute), primary-stat agency (`:442-475`, unlike denatus 261), strong deity voice. No new S1s (242 stays S2, sharpened). Next ID **274**. **➡ NEXT UP = W2-P6b town hub (index/_layout/character/inventory).**
- **S29 (2026-06-04) — W2-P6b `town/{index,_layout,character/index,inventory/index}`, first-hand (logic + cross-checks) → ★ W2-P6 town COMPLETE:** 4 findings (**KV-AUD-274–277**). **274 (S2)** the Character Status screen (`character/index.tsx:94,125`) calls the **unblessed `getDerivedStats`** while combat/dungeon-inventory/familia/town-hub all use `getDerivedStatsWithBlessings` → the canonical stats view diverges from actual combat stats (latent — small today via 270 shrine-only favor + 259 null paragon). **275 (S3)** the Status screen renders the phantom `STAT_INFO.combatEffect` (`:304`, confirms 149 is player-visible) + hardcodes a grade-color palette (`:26-39`) that diverges from `Colors.grade.*` (two palettes). **276 (S3)** town inventory confirms the 3 `as any` casts (026 at `:401,407,446`) and renders 6 phantom (unfillable — no armor data) armor/accessory slots, contradicting the dungeon inventory's honest "future update 🔒" placeholder (267). **277 (S3)** town `_layout` no `gestureEnabled:false` (264 pattern) + `animation:'none'`; `enterDungeon` only `startNewRun()`s `if (!currentRun)` (BUG-013) so a dangling "Menu"-escape run (254) is resumed; Guild-Hall hub card says "seek ascension" (it's at Familia; L10 ASCEND → broken ascension 259/271); positive: permadeath pending-excelia + low-resource warnings before entry. No new S1s. Next ID **278**. **➡ NEXT UP = W2-P7 guildhall + shops (discovery/rep, Gem Exchange BUG-037, sell BUG-038, CHA haggling, buy flow).**
- **S30 (2026-06-04) — W2-P7a `town/guildhall/index.tsx` (1388), first-hand (logic + cross-checks; board/styles skimmed):** 3 findings (**KV-AUD-278–280**). **278 (Info)** the Guild Hall is mostly well-wired — discovery renders hidden/rumored/known/completed (`:80-140`, confirms 134), Gem Exchange filters `category === 'gem'` (`:416`, **BUG-037 resolved**) + fires `resource_sell_items` (`:445,474`, **BUG-030**), the Living-Economy market is surfaced as a Guild Market Board (events + supply pressures, `:350-409`, confirms 111), ascension→familia (`:318`); the one defect — `checkReputationDiscovery(npcReputation.general)` (`:283`) uses the general **SHOP** rep as the single discovery track (confirms 198; semantically odd — shop purchases unlock guild rumors). **279 (S3)** the ascension stat-gate `statsAtGradeD = stats.filter(s => s.points >= 500)` (`:289`) checks raw within-grade points, not grade (comment says "Grade D") → Grade-SSS/low-points fails, Grade-I/high-points passes; likely vestigial (`canLevelUp` is the live gate) but it's the **5th instance of the raw-points-vs-grade bug class** (denatus 261, level-up bonus, PlayerSnapshot 218, boss-encounter, this); deity approval is a free rubber-stamp button (`:297-313`, no favor gate — consistent with 270). **280 (S3)** hardcoded `TIER_COLORS` (`:60-66`) — a 4th off-token palette (≠ level-up's tokens, ≠ character's grade hex). No new S1s. Next ID **281**. **➡ NEXT UP = W2-P7b shops (CHA haggling, sell BUG-038, buy flow, 104/113/105/107).**
- **S31 (2026-06-04) — 📦 Consolidated single-file backups created (token-cutoff safeguard; no audit-progress change):** **`WAVE_1_FINDINGS.md`** (2306 lines / 203 KB — concatenates `02_FINDINGS.md` seeds + `findings/config_lib_hygiene.md` + `findings/stores.md` + `findings/types.md` + `ALL_FINDINGS.md`; spans **KV-AUD-001..219 + the 41 seeds + §1H self-corrections + the 5-S1 tally**) and **`WAVE_2_FINDINGS.md`** (306 lines / 74 KB — `findings/screens.md`, **KV-AUD-220..280**). Built losslessly via `cat` (the canonical `findings/*.md` remain authoritative); verified complete (all boundary findings + PART dividers present, no stray chars). **NEXT UP unchanged = W2-P7b shops; Next ID still 281.**
- **S32 (2026-06-04) — W2-P7b `town/shops/{index,general,equipment,sell,_layout}`, first-hand:** 5 findings (**KV-AUD-281–285**). **★ R1 SELF-CORRECTION — 281 (S2): armor IS buyable AND equippable.** The equipment shop fully sells armor (cards, DEF/MDEF/speedPenalty, "Armor" filter), armor data exists (`baseArmors.ts`), and the **town inventory equips it** (`equipFromInventory:256`, Equip button `:574`); armor **defense applies** (195). So **108 REFUTED** + **my own 276/267 corrected** (armor isn't phantom; the dungeon placeholder is stale). Real defects partial: speed/dodge penalties dead (193), accessory2 unreachable (076). **282 (S2)** the equipment-shop `canAddItem()` guard uses the 50-cap not `BAG_CAPACITY=20` → at 21-49 items (via equip-overflow 070) buy charges gold but `addToInventory` rejects → **silent gold-loss (113 reachable)**. **283 (S3)** BUG-038 resolved (sell excludes gems), CHA haggle consistent display=charge (**219 refuted** for buys), stock meta-keyed (105), sell uses an inline weapon formula not `calculateSellPrice` (**revises 205**), shop sell fires no soul (109). **284 (S3)** more off-token rarity palettes. Back-annotated 276/267 (screens.md) + 108 (stores.md). Next ID 286.
- **S33 (2026-06-04) — W2-P8 `town/blacksmith/{index,repair,upgrade,identify,_layout}`, first-hand → ★ W2-P7+P8 (town economy + blacksmith) COMPLETE:** 3 findings (**KV-AUD-286–288**). **★ R1 REVISION — 286 (S3): `repair.tsx` is a functional SALVAGE screen, not a 705-line no-op (revises 211).** `handleSalvage` (`repair:197-227`) breaks items → materials + gold + rep(+1); titled "SALVAGE"; the hub lists "Salvage Equipment" → `/town/blacksmith/repair` (`index:168-175`). The durability/repair MODEL stays dead (`Weapon.durability` never degrades; no repair service) — 211's spirit holds — but the file is misnamed-functional salvage that **closes the loot loop** (→ upgrade materials). **287 (S3)** upgrade calls `upgradeWeapon` (119 cap-omit confirmed) + **self-heals the registry** (`upgrade:131-132` → mitigates 118); identify calls `identifyWeapon` (114 stale-setState). **288 (Info)** hub uses the canonical `useBlacksmithStore.reputation` (107 — the shop's dup is dead); `repColors` are `Colors.*` tokens (clean). No new S1s (count stays 5). Next ID **289**. **➡ NEXT UP = W2-P9 character-creation.**
- **S34 (2026-06-05) — W2-P9 `character-creation/**` (name/backstory/stats/confirm/equipment/deity), first-hand:** 2 findings (**KV-AUD-289–290**). **289 (S2, extends 156)** both the deity AND backstory `statPenalty` are **displayed but never applied** — `confirm.createCharacter` (`:71-77`) passes only `deity.statBonus`; `getFinalStatValue`/`getStatBonuses`/`getWeaponOptions` add bonuses only; yet `stats.getStatPenalty` (`:73-79`) + the deity detail (`[pantheonId]:219`) SHOW the −X penalty → every character is all-upside, the displayed cost is a lie (MINDSET violation). **290 (Info)** confirms **096** (`initializeDenatus()` at `confirm:80`), **137** (deity picker references no `isDeityUnlocked` → all selectable; "97% locked" refuted), **157** (picker reads `deityAffinity` but `'divine'/'fate'` never match); positives: starter weapon from top-3 stats + a complete atomic creation commit (`createCharacter`+`setPatronDeity`+`initializeDenatus`+`initializeProgress`+resets). No new S1s. Next ID **291**. **➡ NEXT UP (FINAL Wave-2 unit) = W2-P10 tutorial/settings/codex/root.**
- **S35 (2026-06-05) — W2-P10 `tutorial/**`(8) + `settings/*` + `codex/*` + `app/{index,_layout}` (FINAL Wave-2 unit), first-hand:** 7 findings (**KV-AUD-291–297**); **WAVE 2 COMPLETE**. **291 (S3, confirms AUD-012)** the tutorial was trimmed 6→3 by delisting + rewiring `basics→death`, leaving 4 complete orphaned screens — `combat`(391)/`stats`(214)/`falna`(196)/`leveling`(214) = **1015 dead LOC**, a self-connected chain with no inbound edge (exhaustive route grep); smoking gun = stale `totalSteps={6}` vs the live `={2}`. **292 (S3, NEW)** the Haptic-Feedback switch is a placebo — local `useState(true)`, never persisted/read by `useHaptics`. **293 (S3, confirms 144)** volume dual-sourced — `useGameStore.{musicVolume:70,sfxVolume:80}` dead; `useSoundStore` is live (different name + 0-100 vs 0-1 scale). **294 (Info, refutes 010)** settings cheats are `{__DEV__ && …}`-gated → stripped from `expo export`. **295 (Info, resolves 009)** title epitaph is `useMemo`-stable + `clearAllStores()` fires on both New Game branches (`:99,:112`); nit = stale `v0.1.0 — Phase 0` vs settings `1.0.0`. **296 (S3, corroborates 259)** codex `LEVELS=[1..9]` hides ~15 `targetLevel:10` achievements (level9Achievements.ts) + Bestiary/Equipment/Lore are permanent "Soon" stubs + 12-vs-14 domain count. **297 (Info+)** root `ErrorBoundary` graceful-crash wrap is a genuine positive; nits = raw-hex `#060406`/`#c9a84c` in the boundary + no SafeAreaProvider (doc says otherwise). No new S1s (stays 5). Next ID **298**. **➡ NEXT UP = WAVE 3 = `src/data/**` (content layer) + owed 100-floor sim (162).**
- **S36 (2026-06-05) — WAVE 3 STARTED; W3-DC1 `data/monsters/baseMonsters.ts` (1231), first-hand:** 5 findings (**KV-AUD-298–302**) in new `findings/data_combat.md`. Re-sequenced Wave 3 to do the **combat-data (DC) cluster first** (it discharges 163/165/166/168 + supplies the 162 sim inputs), deferring DP/DI/DPr. **298 (S2, NEW)** poison is negated by **~53% of monsters** (17/36 `immunities:['poison']` — every undead/slime/most demons+dragons — + 2 resist) → poison builds non-viable; the **data side of the 189/051 softlock** (the poison-immune set ≈ the bone/armor/ethereal `armorType`s). **299 (S3)** `getRandomMonsterForFloor` (live @ `useCombatStore:269,315`) is unweighted uniform-random over a wide CR band (floor-10 pool spans CR 0.8–3.5) **and** the `biomes` field has **0 readers** repo-wide → dead (compounds 169). **300 (S3, confirms 165)** vestigial `xpValue` on all 36 (5→800). **301 (S3, confirms 052/168)** only 5 suffixes/7 prefixes; suffix `statusEffect` (burn/freeze/poison) dead in combat + non-canonical vs the 190 schism; `the_swift`/`the_undying` passives likely display-only (188). **302 (Info)** **163 LATENT** (min `baseCR` 0.5; min final CR ≈0.2 via `weak` prefix — no CR:0 in data); 8 categories (verify `aberration` + 18 loot tables in `lootPools`, W3-DPr5); baseHP 35→1000 recorded for the 162 sim; deep-floor variety thin (~7 monsters for fl.70–100); the bestiary *data* is the healthiest content layer yet (problems are wiring + balance, not malformed entries). No new S1s (stays 5). Next ID **303**. **➡ NEXT UP = W3-DC2 monster flavor/art/narration → then W3-DC3 weapons (185/153/119).**
- **S38 (2026-06-05) — Audit-HARNESS build started (approved plan: walled-off `audit/` lab → packaged guardrails). Phase-1 keystone + Layer C live.** Stood up isolated `audit/` (own package.json; zod/tsx/fast-check/glob; **never touches game package.json/src/CI**). **Keystone proven:** `audit/sim/smoke.ts` imports the REAL formulas (`calculateDerivedStats`, `createMonsterInstance`) under tsx — numbers match the hand-sim exactly (Goblin@1 HP63; Primordial@86 HP36864/atk6157), independently **re-validating 303**. **Layer C (data integrity) operational:** `audit/schemas/{monsters,validate}.ts` validated 36 monsters + 7 prefixes + 5 suffixes → **3 new findings (KV-AUD-304–306)** the eye missed: **304 (S3)** `elder_dragon` is ice-weak AND ice-immune (contradiction); **305 (S3)** dead union surfaces — armorType `spirit`, 4/9 behaviors, 7/10 statusEffects unused; regeneration triple-dead; **306 (S4)** 10/36 monsters redundantly resist their immune element. Corrections: **301** burn/freeze/poison ARE valid StatusEffectId; **302** 22 (not 18) loot-table refs; **166** category-string alignment unenforced. No new S1s. Next ID **307**. **➡ NEXT UP = extend Layer C schemas to deities/weapons/items + build Layer D real-formula sim; then resume W3-DC2.**
- **S37 (2026-06-05) — Audit-methodology upgrades adopted (user request) + the owed 162 sim EXECUTED:** user picked all 4 upgrades (defect-patterns register, 162 sim, script the data wave, repro+effort on S1/S2). **KV-AUD-303 (S2)** — ran `.audit_tmp/scaling_sim.js` (exact formulas from `Monster.ts`/`Stats.ts`): the run is **unwinnable past ~floor 15** (playable 1–9; lose ~fl.10; one-shot ~fl.20), **~70 floors earlier than 162's "floor 86"** → 162 **corrected & discharged** (root: `monsterLevel≈floor` 1→100 while player level is gated 1→7; monster HP ×670 / atk ×770 vs player ×5). Updated `05 §2` with the executed table; S0 re-confirmed (3 files still untracked). Created **`08_DEFECT_PATTERNS.md`** (groups all 302 findings by ~8 root causes; "fix 1 root → close N"). Going forward: **execute/script** the data wave (W3-DC2+) and attach **Repro + fix-effort** to S1/S2. Next ID **304**. **➡ NEXT UP = W3-DC2 (then DC3 weapons).**
- **S39 (2026-06-08) — W3-DC2 `monsters/{monsterArt,monsterFlavor,index}` + `combat/combatNarration`, first-hand + connectivity-traced:** 4 findings (**KV-AUD-307–310**); content layer is healthy (mirrors the bestiary data). Resumed after a **clinical re-read of all of W1+W2** (owner request) that re-verified the 3 keystone S1s against LIVE source (259/099 confirmed by grep; 303 robust on the defensive wall, soft on the offensive framing — it rests on a *transcribed* sim, flagged for re-run through the real importer) and surfaced two reframings now in the plan (`steady-foraging-bee.md`): a **second thesis** ("the screen is a divergent brain" — runtime→screen duplication: 093/234/251/274/222/282) sharing the *same* missing-effect-resolver root as P1, and the **S1 dependency-lattice** (303 is the keystone that bounds the practical bite of 099/259/190). **307 (S3)** the two status-narration helpers are dead (not imported) → 24 lines of vivid status text never shown; combat uses inline strings. **308 (S3, node-diff)** **12/36 monsters lack ASCII art** — the entire CR 7–16 deep/boss tier (titan/primordial/dragons…) falls back to one generic face (bounded by 303). **309 (S3, discharges 168)** `SUFFIX_FLOOR_GATES`/`isSuffixAllowedOnFloor` is **doubly dead** (0 callers + gate-key vocabulary `burning/venomous/chilling` ∉ suffix ids `of_flame/of_venom/…`); the live gate is the data's `MonsterSuffix.minFloor` (divergent-duplicate, P3). **310 (Info)** positives: `getMonsterFlavorText` wired (`encounter.tsx:60`) with all 7 prefix + 5 suffix display-names matching the flavor keys exactly (no silent-null); 4/6 narration helpers wired AND consumed via `addLogEntry`; `STATUS_DAMAGE` correctly DoT-only; voice on-register; nits = incomplete `index.ts` barrel + flavor shows only on the encounter preview (boss path 265 bypasses it). No new S1/S2 (stays 5). Next ID **311**. **➡ NEXT UP = W3-DC3 weapons** (carries 185/153/119/206; pair with Layer-C zod extension).
- **S40 (2026-06-08) — W3-DC3 `data/weapons/*` (338 weapons), first-hand + harness-executed** (`.audit_tmp/weapon_check.ts` imports the REAL `ALL_WEAPONS` — ground truth, no transcription): 5 findings (**KV-AUD-311–315**). The machine pass **reframed the W1 hybrid cluster** and surfaced **2 new S2s that hit the design pillars hardest**. **311 (S2)** **`specialMechanic` is dead data** on **111/338 weapons incl. all 34 uniques** — grep = type-def + sacred-conversion *writer* only, **0 readers** (no combat handler, no `.base.id` unique switch, no display surface) → CLAUDE pillar #2 "Loot is King" is hollow (the Godslayer crit-chain, World Ender, "Strikes twice per action" neither execute nor show; loot twin of 188/210). **312 (S2)** **dual-stat scaling never engages** — validator: **0/338 weapons use a hybrid `category`** (all single-stat + a `primaryStats:[A,B]` array), so `isHybridMixed = HYBRID_MIXED_CATEGORIES.has(category)` (`useCharacterStore:184`) is **always false** → `physRatio` always 1 → `weaponMagic=0`; the 162 dual-stat weapons' second stat is **inert**, and **STR_INT/AGI_WIS "imbued" hybrids drop their magical half**. The HybridCategory/HYBRID_MIXED apparatus is dead infra (refactor-orphan, P7); **185 type-debt is moot, 153/061 latent** at the data level. **313 (S3)** discharges 119 (store-only; `createWeaponInstance:749` computes `maxOutputCap` right), 206 (base weapons all-canonical damageType), no dup ids; stale `hybridWeapons.ts:4` "320 weapons" comment (~162 real). **314 (S3)** LCK under-provisioned (22 vs 40/cat) + cosmetic `twoHanded` (no off-hand slot). **315 (Info)** cleanest data integrity in the wave (no dup/enum/refint defects); pool wiring + range/twoHanded/attackSpeed display all live; weapon naming/mechanic *voice* outstanding. Both S2s reduce to the **same missing effect-resolver seam** (P1) the audit keeps converging on (cross-links the Thesis-B/lattice notes in `steady-foraging-bee.md`). No new S0/S1 (stays 5); S2 +2. Next ID **316**. **➡ NEXT UP = W3-DC4 bosses** (milestoneBosses 910 + Floors30to60 1400; carries 163/215/218-261-279).
- **S41 (2026-06-08) — W3-DC4 `data/bosses/{milestoneBosses 910, Floors30to60 1400}`, first-hand + harness** (`.audit_tmp/boss_check.ts` imports the REAL `getMilestoneBoss` → 20-boss roster): 5 findings (**KV-AUD-316–320**). **316 (S2)** **boss signature mechanics are display-only** — all 20 bosses define a `mechanic` (Corrosive Shell / Pack Threshold / "fractures into three shadow-pack") rendered at `boss-encounter.tsx:232-233` + "revealed" by Observe, but combat **never reads `boss.mechanic`** and the boss fight is a regular generated monster (265) → the game teaches a puzzle the fight ignores; **reachable pre-wall** for floors 5/10/15 (twin of 311/188, same missing effect-resolver seam). **317 (S3)** CLAUDE gap #6 "floors 30-100 have no bosses" **STALE** — `getMilestoneBoss` aggregates all 3 files; all 20 floors (5-100/5) wired + authored (15 past the 303 wall = authored-yet-unreachable). **318 (S3)** **61 raw-points stat-gates** (`minPoints` 50-150, modal 100×26) → grade-blind boss secret-outcome gating (6th site of 218/261/279, P5). **319 (S3)** `MilestoneBoss` carries **no baseCR/baseMonsterId/stats** → 163 N/A for bosses; favor/archetype-sense reads `PlayerSnapshot` (`adventurerArchetype`/`approachStyle`, varies per 242); forward-note for DC5: `pantheon:'—'` (floor 100) + lowercase `vituna'sta` (floor 85). **320 (Info)** ★ the boss layer is the **strongest content asset in the audit** — pristine refint (20/20 floors, 0 dup ids, 0 floor mismatches, boss achievements resolve 6/6), outstanding dark-fantasy voice at every depth (Vanya→Ulfbiik'ok), sophisticated archetype-sensing design. No new S0/S1 (stays 5); +1 S2 (316). Next ID **321**. **➡ NEXT UP = W3-DC5 bosses** (`Floors65to100` 1485; then W3-DPr progression).
- **S42 (2026-06-08) — W3-DC5 `data/bosses/milestoneBossesFloors65to100` (1485, floors 65-100), first-hand + harness → ★ BOSS CLUSTER COMPLETE:** 4 findings (**KV-AUD-321–324**). **321 (S3, automated; revises 266)** **5 of 8 deep boss-outcome achievements are dangling refs** (`kohr_uwa_split_resolved`/`haitt_lox_war_read`/`ulfkon_naa_stayed`/`vituna_sta_endured`/`ekva_baniyata_cycle_read`, floors 70-90) exist in **0** files → `boss-encounter:151 discoverAchievement(id)` silently no-ops; 266 ("boss achievements wired") holds for floors 5-60 but **breaks at 65-100** — machine-caught (the line-read saw a plausible string). **322 (S3, synthesis 303×316×259×302)** the **authored floor-100 finale Skaervox ("The Lawmaker") is triply unreachable+unimplemented** (~85 floors past the wall; its "the fight ends at 1 HP, there is only the quality of the attempt" mechanic is display-only 316 + a generic deep monster 302; the L10 crown is dead 259) → the entire point of the climb can never be experienced — apex of the theses. **323 (S4)** hygiene (lowercase `vituna'sta`; intentional `pantheon:'—'`; `ChoiceTag` not imported; invented pantheons w/o deity backing). **324 (Info)** ★ Skaervox is the project's best writing; the 20-boss cluster is **complete content** (0 dups, full coverage, 100% authored — the audit's best narrative asset; defects only in the runtime layer beneath). No new S0/S1/S2 (stays 5 S1; 316 the cluster's lone new S2). **★ DC cluster COMPLETE (W3-DC1…DC5 = KV-AUD-298–324).** Next ID **325**. **➡ NEXT UP = W3-DPr1** (`data/achievements/level{1,2,3}` — discharges 196/130 + cross-checks boss refs 321).
- **S43 (2026-06-08) — W3-DPr1 `data/achievements/level{1,2,3}` + registry-wide, first-hand + harness** (`.audit_tmp/ach_check.ts` imports REAL `ALL_ACHIEVEMENTS`): 4 findings (**KV-AUD-325–328**) → **new `findings/data_progression.md`**. R1: re-derived FIRED `RequirementType` set from call sites = exactly 196's 10-fired/11-dead. **325 (S2, discharges/quantifies 196)** **20/123 (16%) uncompletable** via dead types (all 11 used; every level; incl. **4 STANDARD-tier shown as achievable** — `l1_survivor`/`l2_safe_return`/`l3_skilled_fighter`/`l4_safe_deep_return`). **326 (S2, sharpens 130)** **entire MYTHIC tier uncompletable** — 11 mythic+2 legendary use `discoverySource:'undiscovered'` (0 discovery handler) → born hidden → 0 progress catch-22; compounds 178+296 (capstone dead 3 ways). **327 (S3, NEW)** `custom` type has no per-id checker; 20 custom achievements progressed only by type-blind `updateProgress('custom',1)` ×6, all in boss-encounter. **328 (Info)** registry clean (123, 0 dups); **321 re-confirmed** (5 deep boss ids absent); level 1-3 voice excellent. No new S1 (stays 5); +1 S2 (326); 325 discharges 196. **~27% of achievements unreachable** (16% dead-type + 13 undiscoverable). Next ID **329**. **➡ NEXT UP = W3-DPr2** (`achievements/level{4,5,6}` voice; data already validated).
- **S44 (2026-06-08) — W3-DPr2 `data/achievements/level{4,5,6}` (mid tiers), first-hand voice pass** (registry data validated in DPr1): 3 findings (**KV-AUD-329–331**). **329 (S3, NEW)** **conduct/constraint achievements broken by cumulative counters** — `isRequirementMet` correctly implements `eq/lt/gte/…` (positive), but the 6 `eq` + 3 `lt` achievements compare against monotonic lifetime counters: "Untouchable Champion" (`damage_taken eq:0`) = never take ANY damage all game; `lt` constraints true at progress=0 then break → a **3rd uncompletable vector on LIVE types** (vs 325 dead-type / 326 undiscoverable). **330 (S4)** `stat_reach value:4` conflated ("Grade D" vs "4 stats"). **331 (Info)** mid-tier voice strong + values ramp sensibly; **sharper read of 325** — dead-type achievements like "Stealth Master"/"Deity Favored" name **unbuilt systems** (no stealth; favor inert 270) → the achievement layer is partly a fossil of cut features. No new S1/S2 (stays 5); +1 S3 (329). Next ID **332**. **➡ NEXT UP = W3-DPr3** (`achievements/level{7,8,9}+index`).
- **S45 (2026-06-08) — W3-DPr3 `data/achievements/level{7,8,9}+index` (prestige/L10 tail), first-hand → ★ ACHIEVEMENT REGISTRY COMPLETE:** 3 findings (**KV-AUD-332–334**). **332 (S3, sharpens 326)** **Mythic capstone quadruple-dead** — all 11 mythics gate on floors 25-100 (past the 303 wall) + undiscoverable (326) + depend on the dead L10 climax (259) / display-only finale boss (316/322) / unreachable behavements (178); even fixing one lock leaves the others. The achievement apex = densest concentration of audit defects. **333 (S3, NEW)** **`custom value:0` "Without dying" is a no-op** (default `gte` → `≥0` always true; no custom checker 327) → deathless conduct never enforced (reusable broken idiom). **334 (Info)** high-tier voice best in the system (Slaughter→Apocalypse→God Slayer); L10 layer densest dead zone (corroborates 259/296/303/326). **★ 123-achievement registry fully audited (DPr1-3 = KV-AUD-325-334)** — excellent authoring, **~27%+ unreachable across 4 vectors** (325 dead-type / 326 undiscoverable / 327 type-blind custom / 329+333 conduct-constraint). No new S1/S2 (stays 5); +2 S3. Next ID **335**. **➡ NEXT UP = W3-DPr4** (`data/jobs/{jobDefinitions(1338),specializations,index}` — 202 makeJobKey sort-trap).
- **S46 (2026-06-09) — W3-DPr4 `data/jobs/{jobDefinitions 1338, specializations 523, index}`, first-hand + harness** (`.audit_tmp/job_check.ts` imports REAL JOB_DEFINITIONS): 5 findings (**KV-AUD-335–339**). **335 (S2)** **35/56 stat-builds (63%) have NO job** — coverage 21/56 triples; missing-triple chars reach L2 job-less (graceful "Continue" fallback ascension:297, no soft-lock) → breaks "every stat matters" + contradicts doc "2-4/combination". **336 (S2, job-side of 199)** **support archetype starter skills inert** — 13/51 jobs use dead `buff` effect; champion/monk are buff-ONLY no-ops (class-defining skill does nothing). **337 (S3)** 6 base↔spec dup job ids. **338 (S3, ★ REFUTES 202)** getJobsForStats sorts BOTH sides (makeJobKey, "order doesn't matter") → 202 doesn't hold; + sparse specs (9/51 base jobs branch). **339 (Info)** inline starterSkill + StatName weaponCat = no dangling refs; effStat-keyed selection (avoids raw-points class 261/279); strong voice. No new S0/S1 (stays 5); +2 S2 (335/336). Next ID **340**. **➡ NEXT UP = W3-DPr5** (skills+consumables+materials+loot+events+ramifications+weaponRegistry — final data unit).
- **S47 (2026-06-09) — W3-DPr5 (final DC/DPr data unit), first-hand + harness (R1 handler re-derivation) → ★ DC/DPr DATA CLUSTER COMPLETE:** 3 findings (**KV-AUD-340–342**). **340 (S3)** **loot carries 2 dead layers** — monster `lootTable` (22 refs) dead (loot resolves by `MonsterCategory`); pool `materials`/`consumables` fields dead+broken (16 dangling refs, `hide`↔`monster_hide` naming mismatch; combat computes materials its own way); only weapon-cat/gold pool fields live. **341 (S3)** **event/ramification negative-status outcomes partly inert** (weapon_chip `buff`/non-canonical `weapon_damage_down` statusId 190; travel drops them 268; resource drains work, afflictions don't). **342 (Info)** R1 confirms **199 exactly** (buff/damage_percent/flee dead); skill catalog clean (dead impact = job buff-skills 336); **204 LATENT** (no reveal/identify consumable); refint clean; dungeonEvents (53, wired) a genuine Meaningful-Choices win. No new S0/S1/S2 (stays 5); +2 S3. **★ DC1-5 + DPr1-5 COMPLETE = KV-AUD-298-342** (45 findings; 0 new S1, +~6 S2, the rest S3/Info — the data layer is authoring-strong, runtime-holed). Remaining W3: DP pantheons (~24K) + DI items/relics (099 discharge). Next ID **343**. **➡ NEXT UP = W3-DP1 pantheons (or DI items).**
- **S48 (2026-06-09) — Synthesis propagation pass (no new findings; Next ID stays 343):** owner-requested "move everything to the rollups, annotated for a context-less reader." Brought the DC/DPr cluster (298–342) into every respective rollup, **self-contained**: **`ALL_FINDINGS.md`** — §4 register extended 307–342 (one self-contained line per finding) + §3 tally updated to 342 (S2 ~68 / S3 ~93 / S4 ~43; S1 still 5) + a new **Wave-3 DC/DPr reconciliation block** (REFUTED 202; DISCHARGED 162→303/196→325/130→326/168→309/163/213; REFINED 199/204/153-185-061/266; NEW machine-caught breaks 304/321/337/340; the 4 uncompletable vectors) + a one-line cluster verdict. **`04_DATA_INTEGRITY_AND_CONTENT.md`** — rewritten current: its Section-B "checks to RUN" are now **EXECUTED results** (loot→category 340, dup-ids, RequirementType coverage 325, suffix gates 309, baseCR 302, job refs 339), + §C content/voice verdict (EXCELLENT) + §D the new refint findings. **`08_DEFECT_PATTERNS.md`** — Wave-3 members folded per pattern (P1 now runaway-dominant) + **new P11 "Uncompletable-by-construction content."** **`00_EXECUTIVE_SUMMARY.md`** — status header, scorecard, a new "What Wave 3 added" section, health rows (Achievements 🔴 ~27% unreachable; new Jobs row; Weapons/Bosses refreshed), coverage table. **`07`** — new §D data-doc-drift (CLAUDE gap#6 / Job "2-4" / hybrid "320" / "96 achievements" all false). **`03`** — new §6 Wave-3 dead-code/unreachable-content inventory. **`05`** — Wave-3 balance additions (303 executed, 298 poison, 335 job-coverage, 314 LCK, reward systems). CJK-clean; all cite `file:line`/IDs. **Rollups are now current through 342; the next data work (DP pantheons / DI relics → 099 discharge) appends from here.**
- **S49 (2026-06-09) — W3-DI `src/data/items/*` (588 sacred items), harness-first → ★ DISCHARGES THE S1 099:** 4 findings (**KV-AUD-343–346**) in new `findings/data_items.md`. `.audit_tmp/relic_check.ts` imports the REAL relic corpus (490 deity-relic items [245 pairs] + 14 domain + 84 pantheon) and classifies acquisition vs the dead-metric set (evaluator `return 0` cases + the 099 no-writer metrics, re-confirmed `recordParagon`/`incrementEventSuccess`/`incrementSkillUse` = 0 callers). **343 (S1, quantifies 099):** **244/588 (41%) UNOBTAINABLE** + **15 false-unlocks** + **490 deity relics never reveal** (208) + **all 588 passives decorative** (210 — `passiveId` 0 consumers; `passiveDescription` only copied into the dead `specialMechanic` 311) → the reward tier is dead on 3 axes; 099 stays S1, reinforced. **344 (S3):** 107/245 relic weapons non-canonical `damageType` (physical/chaos, quantifies 206). **345 (S3):** acquisition `targetType` nuance dropped by the evaluator. **346 (Info):** ★ the relic tier is the game's most ambitious authoring (245 hand-voiced per-deity conditions + cryptic reveal text) + structurally clean (0 dup ids) — purest "models more than it wires"; doc undercount (245/490 vs "168/336"). No new S0/S1/S2. Next ID **347**. **➡ NEXT UP = W3-DP pantheons** (~24K LOC) → then Wave 4. *(04 §B relic→deity/set→pantheon rows can now flip from PENDING — done below.)*
- **S50 (2026-06-09) — W3-DP `src/data/pantheons/*` (310 deities, 17 active), harness refint + voice sample:** 5 findings (**KV-AUD-347–351**) in new `findings/data_pantheons.md`. **347 (S3, ★ REFUTES seed 028)** maya/inca are complete+valid (export `mayaPantheon`/`incaPantheon`, tsc exit 0, full structure+voice) but **ORPHANED** — the index "TODO: Regenerate with correct interface" is stale (fixed, never re-wired); ~28 authored deities dead (P7); CLAUDE gap #4 reframed (5/7 .tmp wired, maya/inca orphaned). **348 (S3)** 4 dup deity ids (vayu hindu∩persian; fujin/raijin/yama_no_kami japanese∩shinto). **349 (S3, discharges DI-deferred refint)** relic→deity 240/245; 5 dangling = milestone bosses; 66/310 deities relic-less. **350 (S3, doc)** 310 deities/17 pantheons vs docs "168/12" (~2× undercount); all 14 domains+stats valid. **351 (Info)** ★ cleanest core-axis refint + excellent voice. R1 win: almost trusted the index TODO; verifying (tsc + export grep) refuted 028. No new S0/S1/S2 (stays 5). **★ WAVE-3 DATA INTEGRITY EFFECTIVELY COMPLETE (298–351).** Next ID **352**. **➡ NEXT UP = Wave 4** (or optional exhaustive DP voice).
- **S51 (2026-06-09) — ★ WAVE 4 BEGINS — W4-4 tool sweep EXECUTED** (`tsc`/`madge`/`knip` via npx; `.audit_tmp/knip.jsonc` entry:app/**): 4 findings (**KV-AUD-352–355**) in new `findings/wave4.md`. **352 (S3)** `tsc --noEmit`=0 errors = **FALSE GREEN** (S0 files exist on disk + casts suppress 190/166/312/206 → compiler blindfolded; reinforces S0+P4). **353 (S3)** **madge: 23 circular deps** — 3 RUNTIME store cycles (character↔sacred/shop, 4-hop character→shop→consumables→inventory; require()-hacks incomplete) + 20 type-import cycles (relic/boss ↔ aggregator). **354 (S2, ★ validates 03 §6)** **knip CONFIRMS** the audit's orphans (exports: 307/340/309/159/164/148; files: maya/inca 347, types-barrel 217) + NEW: **starterSkills.ts dead** (extends 342), **7 component orphans** (DeityFilters/ActionModal/EmphasisText/TowerWarningModal/StatTooltip+barrels — pre-validates W4-1), **6 dead barrels**; knip CAN'T flag dead app/** routes (entries) → 260/291 needed manual route-graph. **355 (Info)** sweep proves "execute, don't just read" (~10 reading findings machine-re-derived); ship knip.jsonc+madge → `audit/integration/` + CI gate (closes P7/P4). No new S0/S1 (stays 5). Next ID **356**. **➡ NEXT UP = W4-1 components** (53 files; knip's 7 orphans as head-start) → W4-2/3/5.
- **S52 (2026-06-09) — W4-1 `src/components/**` (49 .tsx), machine pass + sample read:** 4 findings (**KV-AUD-356–359**) in `findings/wave4.md`. **356 (S3, discharges 033)** 0/49 components use `React.memo` (1 useMemo/useCallback) → no memo → heavy screens re-render whole tree (compounds 230/238/256; mitigated by native-driver ref-animations). **357 (S3, discharges seed 008)** no custom font (no useFonts/Font.loadAsync; 'monospace' for ASCII) → gothic-serif aesthetic unimplemented (system fonts). **358 (S3)** 66 hex across 7 components + 7 knip-confirmed dead components (ActionModal a refactor-orphan) + MonsterDisplay 4 dead behaviorPattern tells (305). **359 (Info)** ★ otherwise well-built (token-disciplined, native-driver animations, dumb-presentational). No new S0/S1/S2 (stays 5). All 49 machine-covered + MonsterDisplay first-hand; exhaustive per-component read deferred (low-yield). Next ID **360**. **➡ NEXT UP = W4-2 constants/hooks** → W4-3 config/docs → W4-5 synthesis.
- **S53 (2026-06-09) — W4-2 (`constants`/`hooks`) + W4-3 (config/hygiene), first-hand:** 4 findings (**KV-AUD-360–363**). **360 (S2)** ★ **GameConstants ~80% DEAD** — only 4/20 configs imported (Combat/Loot/ZONE_NAMES/DENATUS_WILAK_ALIGNMENT); 16 dead (DerivedStatFormulas/WeaponBalance/MonsterBalance/Deity/…) → "single source of balance truth / never hardcode" law systematically violated; balance is untunable from the documented source (definitive 003/151 discharge). **361 (S3)** Typography declares no serif font (357 confirmed at source) + useHaptics fires unconditionally (292 placebo confirmed). **362 (Info)** Colors = solid 13-group token system; off-token palettes diverge from EXISTING tokens (mechanical fix). **363 (S3)** 17 junk files litter+track repo root (012 confirmed; the git-status churn); config present. No new S1. **★ WAVE-4 FIRST-HAND READING COMPLETE** (W4-1/2/3 + W4-4 sweep). Next ID **364**. **➡ NEXT UP = W4-5 FINAL SYNTHESIS** (last unit).
- **S54 (2026-06-09) — ★ W4-5 FINAL SYNTHESIS — THE AUDIT IS COMPLETE (Waves 1–4, KV-AUD-001–363):** report-only, **no new finding IDs** (Next ID stays 364). Regenerated **`06_REMEDIATION_BACKLOG.md`** from scratch — leverage-ordered against the 08 patterns + current IDs: **P0 ship-blockers** (303 unwinnable-scaling · 001 build-break · CI gates), **P1 architectural seams** (#4 the ONE effect-resolver — closes ~30 findings · #5 type-tightening · #6 reconnect GameConstants 360), **P2 the 5 S1s + permadeath**, **P3 content completion** (achievements 27% / jobs 63% / sacred 41% / maya-inca wiring), **P4 cleanup** (knip/madge CI · junk files · doc regen) + the 5-highest-leverage box + the "NOT broken, build on it" note. Finalized **`02_FINDINGS.md`** (★ seed 028 REFUTED→347; 008→357/361, 033→356, 035→358/362, 012→363, 003→360; header → audit-complete). Final **`00_EXECUTIVE_SUMMARY.md`** touch (status → AUDIT COMPLETE; 363 findings; 6 R1 self-corrections; the bottom-line "wire what's built, gate the floor" thesis). **★★ AUDIT COMPLETE: 363 findings · 1 S0 · 5 S1 · ~70 S2 · ~95 S3 · ~50 Info · 0 game-code edits (report-only).** Optional remaining (low-yield, owner's call): exhaustive 310-deity + per-component voice reads. Resume pointer below is now a COMPLETE marker.
- **S55 (2026-06-09) — ★ EXHAUSTIVE per-deity + per-component reads (owner request: "no stones unturned")** — 8 findings (**KV-AUD-364–371**). **DEITIES (364-367, all 310 read first-hand via content-integrity script + full voice dump):** **364 (S3)** deity `statBonus` **unnormalized** — older pantheons +10/-5, newer (persian/shinto/vodou/polynesian/yoruba) +12 to +15/-5 to -7 → patron pick has a hidden min-max optimum (Verethragna +15STR). **365 (S4)** ~71 deities (the 5 newer pantheons + omoikane) have **stray embedded quotes** in loreSnippet → render `""…"`. **366 (S4)** 3 dup ability-descriptions + 4 dup ability-names + Astaroth in 2 demonic pantheons. **367 (Info)** ★ EXHAUSTIVE verdict: 310/310 read — **0 stubs/dup-lore/placeholders/cross-deity-errors/misspellings**; uniformly excellent on-MINDSET voice (newer pantheons richer first-person) — a top-tier content asset. **COMPONENTS (368-371, all 49 examined via complexity scan + deep-read of every logic-bearing one):** **368 (S3)** off-token "second palette" is **SOURCED** in BlessingCeremony (`DOMAIN_GLOW_COLORS` ≠ `Colors.domain.*`) + NarrativeLog (`getEntryStyle`). **369 (S3)** HP/SP bars (`AnimatedBar`, `useNativeDriver:false` for width+color) + typewriter per-char setState animate on the **JS thread** — the concrete 033/05/356 perf instance. **370 (S4)** telephone glyph for `knowledge` domain symbol + `formatNarrativeMessage` damage-emphasis is a no-op. **371 (Info)** ★ EXHAUSTIVE verdict: 49/49 examined — clean, well-architected, animation-efficient presentational tier (ref-based Animated.Values, native-driver where possible, token-disciplined, dumb-presentational); defects entirely systemic + the 7 orphans. No new S0/S1/S2 (stays 5). **★★ THE AUDIT IS NOW EXHAUSTIVELY COMPLETE: KV-AUD-001–371, every deity + every component read.** Next ID **372**. **✅ PER-WAVE CONSOLIDATION DONE** — `WAVE_{1,2,3,4}_FINDINGS.md` each now carry a **"for team analysis" header** (scope · severity tally · the wave's headlines · `→ 06` fix-pointers) followed by the **lossless cat** of that wave's canonical findings, so the team can take each wave independently and validate how to fix it. (W1 engine 001-219 · W2 screens 220-297 · W3 data 298-351+364-367 · W4 remainder 352-363+368-371.) **★★★ AUDIT FULLY DELIVERED.**
